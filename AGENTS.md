# Agent Instructions

Notes for AI agents working in this repository.

## Code review

This repo sets `greptile.json` to `{"skipReview": "AUTOMATIC"}` so Greptile only reviews a pull request on demand, when the `second-opinion` label is applied; CodeRabbit is the automatic reviewer for every PR.
