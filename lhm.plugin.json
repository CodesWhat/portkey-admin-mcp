{
	"identifier": "codeswhat-portkey-admin-mcp",
	"name": "Portkey Admin MCP Server",
	"version": "0.11.0",
	"description": "Portkey Admin API MCP server with current control-plane coverage, 178 tools, and Prisma AIRS interoperability guidance.",
	"author": "CodesWhat",
	"authorUrl": "https://github.com/CodesWhat",
	"category": "development",
	"homepage": "https://github.com/CodesWhat/portkey-admin-mcp",
	"tags": [
		"mcp",
		"portkey",
		"admin-api",
		"prompts",
		"analytics",
		"observability",
		"gateway",
		"prisma-airs"
	],
	"prompts": [
		{
			"name": "plan_portkey_admin_workflow",
			"title": "Plan Portkey Admin Workflow",
			"description": "Create a concise, safe plan for a Portkey Admin API task using this MCP server.",
			"arguments": [
				{
					"name": "task",
					"description": "Portkey admin task to plan, such as promoting a prompt",
					"required": true
				},
				{
					"name": "area",
					"description": "Optional Portkey area, such as prompts, analytics, configs, or users",
					"required": false
				}
			]
		}
	],
	"resources": [
		{
			"name": "workflow-guide",
			"title": "Portkey Admin Workflow Guide",
			"uri": "portkey-admin://docs/workflow-guide",
			"description": "Operational guidance for using Portkey Admin MCP tools safely and effectively.",
			"mimeType": "text/markdown",
			"annotations": {
				"audience": ["assistant"],
				"priority": 0.8
			}
		}
	],
	"tools": [
		{
			"name": "list_all_users",
			"description": "List accepted org users with id, name, email, role, and timestamps. Use this to find a user_id before get_user, update_user, delete_user, or add_workspace_member; use list_user_invites for pending invitations. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"minimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Number of results per page (max 100)",
						"type": "integer",
						"minimum": 0,
						"maximum": 100
					},
					"role": {
						"description": "Filter by organisation role",
						"type": "string",
						"enum": ["admin", "member", "owner"]
					},
					"email": {
						"description": "Filter by exact email",
						"type": "string",
						"format": "email",
						"pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$"
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "invite_user",
			"description": "Invite a new org user and optionally provision workspace access and an API key in one call. Workspace assignments apply only after acceptance; use add_workspace_member or update_workspace_member later for follow-up changes.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"email": {
						"type": "string",
						"format": "email",
						"pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$",
						"description": "Email address of the user to invite"
					},
					"role": {
						"type": "string",
						"enum": ["admin", "member"],
						"description": "Organization-level role: 'admin' for full access, 'member' for limited access"
					},
					"first_name": {
						"description": "User's first name",
						"type": "string"
					},
					"last_name": {
						"description": "User's last name",
						"type": "string"
					},
					"workspaces": {
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"id": {
									"type": "string",
									"description": "Workspace ID/slug where the user will be granted access"
								},
								"role": {
									"type": "string",
									"enum": ["admin", "member", "manager"],
									"description": "Workspace-level role: 'admin' for full access, 'manager' for workspace management, 'member' for basic access"
								}
							},
							"required": ["id", "role"]
						},
						"description": "List of workspaces and corresponding roles to grant to the user"
					},
					"workspace_api_key_details": {
						"description": "Optional API key to be created for the user",
						"type": "object",
						"properties": {
							"name": {
								"description": "Name of the API key to be created",
								"type": "string"
							},
							"expiry": {
								"description": "Expiration date for the API key (ISO8601 format)",
								"type": "string"
							},
							"metadata": {
								"description": "Additional metadata key-value pairs for the API key",
								"type": "object",
								"propertyNames": {
									"type": "string"
								},
								"additionalProperties": {
									"type": "string"
								}
							},
							"scopes": {
								"type": "array",
								"items": {
									"type": "string"
								},
								"description": "List of permission scopes for the API key"
							}
						},
						"required": ["scopes"]
					}
				},
				"required": ["email", "role", "workspaces"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "get_user_stats",
			"description": "Return per-user request and cost analytics for a required time range. This is usage-by-user, not population metrics; use get_users_analytics for active-user or cohort trends. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"status_code": {
						"description": "Filter by specific HTTP status codes (comma-separated)",
						"type": "string"
					},
					"virtual_keys": {
						"description": "Filter by specific virtual key slugs (comma-separated)",
						"type": "string"
					},
					"page_size": {
						"description": "Number of results per page (max 100)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_user",
			"description": "Get one accepted user by id and return their profile, role, and timestamps. Use list_all_users to find the id if you only have a name or email, and get_user_invite for pending invitations. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"user_id": {
						"type": "string",
						"description": "The user ID to retrieve"
					}
				},
				"required": ["user_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_user",
			"description": "Update a user's first name, last name, or organization role by id. Email and workspace roles are not editable here; use update_workspace_member for workspace membership changes.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"user_id": {
						"type": "string",
						"description": "The user ID to update"
					},
					"first_name": {
						"description": "New first name",
						"type": "string"
					},
					"last_name": {
						"description": "New last name",
						"type": "string"
					},
					"role": {
						"description": "New organization-level role",
						"type": "string",
						"enum": ["admin", "member"]
					}
				},
				"required": ["user_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "delete_user",
			"description": "Delete a user from the org by id. This is permanent, removes org and workspace memberships, revokes API keys, and ends active sessions; use delete_user_invite for pending invites instead.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"user_id": {
						"type": "string",
						"description": "The user ID to delete"
					}
				},
				"required": ["user_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_user_invites",
			"description": "List pending and sent invitations with id, email, role, status, and expiry. Use this to check invite state; use list_all_users for users who already accepted. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Number of results per page (max 100)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					},
					"role": {
						"description": "Filter invitations by organisation role",
						"type": "string",
						"enum": ["admin", "member"]
					},
					"email": {
						"description": "Filter by invited email",
						"type": "string",
						"format": "email",
						"pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$"
					},
					"status": {
						"description": "Filter invitations by lifecycle status",
						"type": "string",
						"enum": ["pending", "cancelled", "accepted", "expired"]
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_user_invite",
			"description": "Get one invitation by invite id and return its email, role, status, and expiry. Use this for pending invites only; use get_user for accepted users.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"invite_id": {
						"type": "string",
						"description": "The invite ID to retrieve"
					}
				},
				"required": ["invite_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "delete_user_invite",
			"description": "Delete a pending invite and revoke its invite link. This does not affect existing users; use delete_user for full user removal.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"invite_id": {
						"type": "string",
						"description": "The invite ID to delete"
					}
				},
				"required": ["invite_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "resend_user_invite",
			"description": "Resend the email for a pending invite that has not been accepted, unlike invite_user which creates a new invite. This sends a fresh email without modifying the invite record, expiry, or role; use get_user_invite first if you are unsure whether the invite still exists and list_user_invites to discover invite_ids.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"invite_id": {
						"type": "string",
						"description": "The invite ID to resend"
					}
				},
				"required": ["invite_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_scim_workspace_mappings",
			"description": "List identity-provider SCIM group mappings that automatically grant Portkey workspace roles. Use it to audit provisioned access or obtain mapping_id before delete_scim_workspace_mapping; filter by workspace, group, or role and page through large directories. This reads mappings only and does not query individual workspace members. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"workspace_id": {
						"description": "Return only mappings for this Portkey workspace ID",
						"type": "string"
					},
					"scim_group_id": {
						"description": "Return only mappings for this identity-provider SCIM group ID",
						"type": "string"
					},
					"role": {
						"description": "Return only mappings that grant this workspace role",
						"type": "string",
						"enum": ["admin", "member", "manager"]
					},
					"page": {
						"description": "Zero-based results page to retrieve; the first page is 0. Unlike current_page used by other list tools in this API (which is 1-based), do not pass 1 for the first page here.",
						"type": "integer",
						"minimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Mappings per page, from 1 through 100",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "List SCIM Workspace Mappings",
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "create_scim_workspace_mapping",
			"description": "Map one identity-provider SCIM group to a Portkey workspace role so current and future group members receive access automatically. Provide exactly one of scim_group_id or scim_group_name; a name can pre-create the Portkey SCIM group before the IdP provisions it. Use list_scim_groups to discover existing groups and list_workspaces for the workspace ID. This changes access provisioning and is distinct from add_workspace_member, which grants one user directly. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"workspace_id": {
						"type": "string",
						"description": "Portkey workspace ID that the SCIM group should access"
					},
					"role": {
						"type": "string",
						"enum": ["admin", "member", "manager"],
						"description": "Workspace role automatically granted to group members"
					},
					"scim_group_id": {
						"description": "Existing identity-provider SCIM group ID; provide this or scim_group_name, but not both",
						"type": "string",
						"minLength": 1
					},
					"scim_group_name": {
						"description": "Existing SCIM group display name; provide this or scim_group_id, but not both",
						"type": "string",
						"minLength": 1
					}
				},
				"required": ["workspace_id", "role"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Create SCIM Workspace Mapping",
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "delete_scim_workspace_mapping",
			"description": "Delete a SCIM group-to-workspace mapping by mapping_id and stop future group updates from affecting that workspace. Existing provisioned members remain in the workspace and must be managed separately. Inspect list_scim_workspace_mappings first; this does not delete the identity-provider group or the Portkey workspace. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"mapping_id": {
						"type": "string",
						"description": "SCIM workspace mapping ID from list_scim_workspace_mappings"
					}
				},
				"required": ["mapping_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Delete SCIM Workspace Mapping",
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "list_scim_groups",
			"description": "Search and page through identity-provider groups synchronized to Portkey over SCIM. Use this to resolve a group ID or exact display name before create_scim_workspace_mapping; it reads directory groups only and does not show their workspace mappings or individual members. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"search": {
						"description": "Case-insensitive text to match against SCIM group names",
						"type": "string"
					},
					"page": {
						"description": "Zero-based results page to retrieve; the first page is 0. Unlike current_page used by other list tools in this API (which is 1-based), do not pass 1 for the first page here.",
						"type": "integer",
						"minimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "SCIM groups per page, from 1 through 100",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "List SCIM Groups",
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "list_workspaces",
			"description": "List workspaces with id, name, slug, default settings, and timestamps. Use this to find a workspace_id before get_workspace, update_workspace, add_workspace_member, or remove_workspace_member.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"page_size": {
						"description": "Number of workspaces to return per page (max 100)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					},
					"current_page": {
						"description": "Page number to retrieve when results are paginated",
						"type": "integer",
						"minimum": 0,
						"maximum": 9007199254740991
					},
					"name": {
						"description": "Case-sensitive name filter",
						"type": "string"
					},
					"exact_name": {
						"description": "Exact workspace name filter",
						"type": "string"
					},
					"status": {
						"description": "Filter by workspace lifecycle status",
						"type": "string",
						"enum": ["active", "archived"]
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_workspace",
			"description": "Get one workspace by id and return its full details, including defaults and the complete member list. Use this when you need membership detail; use list_workspaces for an overview.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"workspace_id": {
						"type": "string",
						"description": "The unique identifier of the workspace to retrieve. This can be found in the workspace's URL or from the list_workspaces tool response"
					}
				},
				"required": ["workspace_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "create_workspace",
			"description": "Create a workspace to isolate resources, API keys, and team members. If slug is omitted it is auto-generated from the name; returns the new workspace id, name, and slug.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"name": {
						"type": "string",
						"description": "Name of the workspace"
					},
					"slug": {
						"description": "URL-friendly slug (auto-generated if not provided)",
						"type": "string"
					},
					"description": {
						"description": "Description of the workspace",
						"type": "string"
					},
					"is_default": {
						"description": "Set as default workspace (1 = yes, 0 = no)",
						"type": "number"
					},
					"metadata": {
						"description": "Custom metadata key-value pairs",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {
							"type": "string"
						}
					},
					"users": {
						"description": "Existing user IDs to add",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"usage_limits": {
						"description": "Initial workspace usage-limit settings",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"credit_limit": {
									"description": "Maximum cost or token usage for the workspace",
									"type": "number",
									"exclusiveMinimum": 0
								},
								"type": {
									"description": "Whether the limit measures cost or tokens",
									"type": "string",
									"enum": ["cost", "tokens"]
								},
								"alert_threshold": {
									"description": "Percentage of the limit that triggers an alert",
									"type": "number",
									"exclusiveMinimum": 0
								},
								"periodic_reset": {
									"description": "Built-in weekly or monthly reset cadence",
									"anyOf": [
										{
											"type": "string",
											"enum": ["monthly", "weekly"]
										},
										{
											"type": "null"
										}
									]
								},
								"periodic_reset_days": {
									"description": "Custom reset cadence in days, from 1 through 365",
									"anyOf": [
										{
											"type": "integer",
											"minimum": 1,
											"maximum": 365
										},
										{
											"type": "null"
										}
									]
								},
								"next_usage_reset_at": {
									"description": "Next reset timestamp in ISO 8601 format",
									"anyOf": [
										{
											"type": "string",
											"format": "date-time",
											"pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z|([+-](?:[01]\\d|2[0-3]):[0-5]\\d)))$"
										},
										{
											"type": "null"
										}
									]
								}
							}
						}
					},
					"rate_limits": {
						"description": "Initial workspace rate-limit settings",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"type": {
									"description": "Whether the limit counts requests or tokens",
									"type": "string",
									"enum": ["requests", "tokens"]
								},
								"unit": {
									"description": "Rate window: requests per day, hour, or minute",
									"type": "string",
									"enum": ["rpd", "rph", "rpm"]
								},
								"value": {
									"description": "Maximum count in the selected rate window",
									"type": "integer",
									"minimum": 0,
									"maximum": 9007199254740991
								}
							}
						}
					}
				},
				"required": ["name"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "update_workspace",
			"description": "Update a workspace's name, slug, description, default flag, or metadata by id, unlike update_workspace_member which changes role assignments within a workspace. Only provided fields change and updates take effect immediately; changing the slug can break URLs, API key references, and other external links, so confirm no dependencies first.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"workspace_id": {
						"type": "string",
						"description": "The workspace ID to update"
					},
					"name": {
						"description": "New name for the workspace",
						"type": "string"
					},
					"slug": {
						"description": "New slug for the workspace",
						"type": "string"
					},
					"description": {
						"description": "New description",
						"type": "string"
					},
					"is_default": {
						"description": "Set as default workspace (1 = yes, 0 = no)",
						"type": "number"
					},
					"metadata": {
						"description": "New metadata key-value pairs",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {
							"type": "string"
						}
					},
					"input_guardrails": {
						"description": "Guardrail IDs applied to workspace model inputs",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"output_guardrails": {
						"description": "Guardrail IDs applied to workspace model outputs",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"user_api_key_config": {
						"description": "Default config slug for workspace-user API keys, or null to clear",
						"anyOf": [
							{
								"type": "string"
							},
							{
								"type": "null"
							}
						]
					},
					"usage_limits": {
						"description": "Replacement workspace usage-limit settings",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"credit_limit": {
									"description": "Maximum cost or token usage for the workspace",
									"type": "number",
									"exclusiveMinimum": 0
								},
								"type": {
									"description": "Whether the limit measures cost or tokens",
									"type": "string",
									"enum": ["cost", "tokens"]
								},
								"alert_threshold": {
									"description": "Percentage of the limit that triggers an alert",
									"type": "number",
									"exclusiveMinimum": 0
								},
								"periodic_reset": {
									"description": "Built-in weekly or monthly reset cadence",
									"anyOf": [
										{
											"type": "string",
											"enum": ["monthly", "weekly"]
										},
										{
											"type": "null"
										}
									]
								},
								"periodic_reset_days": {
									"description": "Custom reset cadence in days, from 1 through 365",
									"anyOf": [
										{
											"type": "integer",
											"minimum": 1,
											"maximum": 365
										},
										{
											"type": "null"
										}
									]
								},
								"next_usage_reset_at": {
									"description": "Next reset timestamp in ISO 8601 format",
									"anyOf": [
										{
											"type": "string",
											"format": "date-time",
											"pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z|([+-](?:[01]\\d|2[0-3]):[0-5]\\d)))$"
										},
										{
											"type": "null"
										}
									]
								}
							}
						}
					},
					"rate_limits": {
						"description": "Replacement workspace rate-limit settings",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"type": {
									"description": "Whether the limit counts requests or tokens",
									"type": "string",
									"enum": ["requests", "tokens"]
								},
								"unit": {
									"description": "Rate window: requests per day, hour, or minute",
									"type": "string",
									"enum": ["rpd", "rph", "rpm"]
								},
								"value": {
									"description": "Maximum count in the selected rate window",
									"type": "integer",
									"minimum": 0,
									"maximum": 9007199254740991
								}
							}
						}
					}
				},
				"required": ["workspace_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "delete_workspace",
			"description": "Delete a workspace by id. This is permanent and removes the workspace, its members, configs, API keys, and resources.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"workspace_id": {
						"type": "string",
						"description": "The workspace ID to delete"
					}
				},
				"required": ["workspace_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "add_workspace_member",
			"description": "Add an existing org user to a workspace with a role. Requires a UUID user_id; use list_all_users to find it, and invite_user first if the person is not yet in the org.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"workspace_id": {
						"type": "string",
						"description": "The workspace ID to add the member to"
					},
					"user_id": {
						"type": "string",
						"format": "uuid",
						"pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
						"description": "The user ID to add (must be a valid UUID from list_all_users, not an email address)"
					},
					"role": {
						"type": "string",
						"enum": ["admin", "member", "manager"],
						"description": "Role in the workspace"
					}
				},
				"required": ["workspace_id", "user_id", "role"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_workspace_members",
			"description": "List every member in a workspace with organization role, workspace role, status, and timestamps. Use this to find a user_id before get_workspace_member, update_workspace_member, or remove_workspace_member.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"workspace_id": {
						"type": "string",
						"description": "The workspace ID to list members for"
					},
					"current_page": {
						"description": "Zero-based page number",
						"type": "integer",
						"minimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Number of members per page",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 9007199254740991
					},
					"role": {
						"description": "Filter by workspace role",
						"type": "string",
						"enum": ["admin", "manager", "member"]
					},
					"email": {
						"description": "Filter by member email",
						"type": "string",
						"format": "email",
						"pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$"
					}
				},
				"required": ["workspace_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_workspace_member",
			"description": "Get one workspace member by workspace_id and user_id. Use this when you already know both IDs; use list_workspace_members to browse the full roster.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"workspace_id": {
						"type": "string",
						"description": "The workspace ID"
					},
					"user_id": {
						"type": "string",
						"format": "uuid",
						"pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
						"description": "The user ID to retrieve (must be a valid UUID from list_all_users, not an email address)"
					}
				},
				"required": ["workspace_id", "user_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_workspace_member",
			"description": "Update a workspace member's role by workspace_id and user_id. Only the role changes here; use list_workspace_members or get_workspace_member to confirm the current assignment first.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"workspace_id": {
						"type": "string",
						"description": "The workspace ID"
					},
					"user_id": {
						"type": "string",
						"format": "uuid",
						"pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
						"description": "The user ID to update (must be a valid UUID from list_all_users, not an email address)"
					},
					"role": {
						"type": "string",
						"enum": ["admin", "member", "manager"],
						"description": "New role in the workspace"
					}
				},
				"required": ["workspace_id", "user_id", "role"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "remove_workspace_member",
			"description": "Remove a user from a workspace and revoke workspace access. This does not delete the user from the organization; use delete_user for full removal.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"workspace_id": {
						"type": "string",
						"description": "The workspace ID"
					},
					"user_id": {
						"type": "string",
						"format": "uuid",
						"pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
						"description": "The user ID to remove"
					}
				},
				"required": ["workspace_id", "user_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_configs",
			"description": "List configs in the org with id, slug, name, status, workspace, and timestamps. Use this summary view to find a slug; use get_config for the full routing, cache, retry, and target settings before updating or deleting.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Number of results per page (max 100)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_config",
			"description": "Get one config by slug and return its routing, cache, retry, and target settings. Requires a known slug; use list_configs to discover one before editing.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"slug": {
						"type": "string",
						"description": "The unique identifier (slug) of the configuration to retrieve. This can be found in the configuration's URL or from the list_configs tool response"
					}
				},
				"required": ["slug"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "create_config",
			"description": "Create a config that defines routing, cache, retry, and targets for requests; use update_config to modify an existing one and list_config_versions for history. At least one setting is required, new configs become active immediately once referenced by a key or prompt, and the call returns the new id and version_id.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"name": {
						"type": "string",
						"description": "Name for the new configuration"
					},
					"workspace_id": {
						"description": "Workspace ID to create config in",
						"type": "string"
					},
					"cache_mode": {
						"description": "Cache mode: 'simple' or 'semantic'",
						"type": "string",
						"enum": ["simple", "semantic"]
					},
					"cache_max_age": {
						"description": "Cache max age in seconds",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"retry_attempts": {
						"description": "Number of retry attempts (1-5)",
						"type": "number",
						"exclusiveMinimum": 0,
						"maximum": 5
					},
					"retry_on_status_codes": {
						"description": "HTTP status codes to retry on (e.g., [429, 500, 502, 503])",
						"type": "array",
						"items": {
							"type": "number"
						}
					},
					"strategy_mode": {
						"description": "Routing strategy: 'loadbalance' or 'fallback'",
						"type": "string",
						"enum": ["loadbalance", "fallback"]
					},
					"targets": {
						"description": "Array of target providers with virtual keys",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"provider": {
									"description": "Provider slug to route requests to",
									"type": "string"
								},
								"virtual_key": {
									"description": "Virtual key slug to use for this routing target",
									"type": "string"
								}
							}
						}
					}
				},
				"required": ["name"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "update_config",
			"description": "Update a config by slug; every call creates a new config version rather than overwriting, so earlier versions stay recoverable via list_config_versions. Only provided fields change: name, status, and all routing/cache/retry settings (cache_mode, cache_max_age, retry_attempts, retry_on_status_codes, strategy_mode, targets) are editable, while the slug stays fixed. Changes apply immediately to every API key and prompt referencing the config. Returns the update acknowledgement and new version_id.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"slug": {
						"type": "string",
						"description": "Configuration slug to update"
					},
					"name": {
						"description": "New name for the configuration",
						"type": "string"
					},
					"status": {
						"description": "Configuration status",
						"type": "string",
						"enum": ["active", "inactive"]
					},
					"cache_mode": {
						"description": "Cache mode: 'simple' or 'semantic'",
						"type": "string",
						"enum": ["simple", "semantic"]
					},
					"cache_max_age": {
						"description": "Cache max age in seconds",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"retry_attempts": {
						"description": "Number of retry attempts (1-5)",
						"type": "number",
						"exclusiveMinimum": 0,
						"maximum": 5
					},
					"retry_on_status_codes": {
						"description": "HTTP status codes to retry on",
						"type": "array",
						"items": {
							"type": "number"
						}
					},
					"strategy_mode": {
						"description": "Routing strategy",
						"type": "string",
						"enum": ["loadbalance", "fallback"]
					},
					"targets": {
						"description": "Array of target providers",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"provider": {
									"description": "Provider slug to route requests to",
									"type": "string"
								},
								"virtual_key": {
									"description": "Virtual key slug to use for this routing target",
									"type": "string"
								}
							}
						}
					}
				},
				"required": ["slug"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": false
			}
		},
		{
			"name": "delete_config",
			"description": "Delete a config by slug. This is permanent, removes all versions, and breaks anything still pointing at that slug; check list_config_versions first.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"slug": {
						"type": "string",
						"description": "Configuration slug to delete"
					}
				},
				"required": ["slug"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_config_versions",
			"description": "List every version of a config with version_id, structured config payload, updater, and timestamp. Use this to audit history or compare revisions before update_config or delete_config.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"slug": {
						"type": "string",
						"description": "Configuration slug to list versions for"
					}
				},
				"required": ["slug"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "list_deployments",
			"description": "Enterprise-gated. List registered self-hosted Gateway deployments with status, type, default state, and connection health. Use this before get_deployment, update_deployment, or archive_deployment to resolve a deployment ID. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"organisation_id": {
						"description": "Filter by organisation UUID",
						"type": "string"
					},
					"status": {
						"description": "Filter by deployment status",
						"type": "string",
						"enum": ["active", "archived"]
					},
					"type": {
						"description": "Filter by deployment type",
						"type": "string",
						"enum": ["production", "non_production"]
					},
					"workspace_slug": {
						"description": "Filter by one or more workspace slugs",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"search": {
						"description": "Search deployment names",
						"type": "string"
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "register_deployment",
			"description": "Enterprise-gated. Register a self-hosted Gateway deployment when onboarding a new control-plane target; use list_deployments for existing registrations. The response can contain authentication and registry credentials exactly once, exposed to this MCP transcript, so store them securely immediately and never log them. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"name": {
						"type": "string",
						"minLength": 1,
						"description": "Deployment display name"
					},
					"organisation_id": {
						"description": "Owning organisation UUID",
						"type": "string"
					},
					"slug": {
						"description": "Optional deployment slug",
						"type": "string",
						"minLength": 1,
						"maxLength": 50
					},
					"type": {
						"description": "Production or non-production deployment type",
						"type": "string",
						"enum": ["production", "non_production"]
					},
					"deployment_config": {
						"description": "Gateway deployment configuration",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"is_default": {
						"description": "Make this the default deployment",
						"type": "boolean"
					},
					"gateway_base_url": {
						"description": "Self-hosted Gateway base URL",
						"type": "string",
						"format": "uri"
					},
					"mcp_gateway_base_url": {
						"description": "MCP Gateway base URL",
						"type": "string",
						"format": "uri"
					},
					"is_dataservice_hosted": {
						"description": "Whether the deployment hosts its own data service",
						"anyOf": [
							{
								"type": "number",
								"const": 0
							},
							{
								"type": "number",
								"const": 1
							}
						]
					},
					"is_playground_proxy_allowed": {
						"description": "Whether Playground proxy traffic is allowed",
						"anyOf": [
							{
								"type": "number",
								"const": 0
							},
							{
								"type": "number",
								"const": 1
							}
						]
					},
					"workspaces_allowed": {
						"description": "Workspace slugs this deployment may serve; empty allows all",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"jwt_subs_allowed": {
						"description": "JWT subject values allowed to use the deployment",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"jwt_sub_workspace_mapping": {
						"description": "JWT subject to workspace-slug mapping",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {
							"type": "string"
						}
					}
				},
				"required": ["name"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "get_deployment",
			"description": "Enterprise-gated. Get one registered Gateway deployment by UUID, or use self when authenticating as that deployment. Use list_deployments to resolve an ID; read responses contain only masked authentication and registry credential values. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"minLength": 1,
						"description": "Deployment UUID or self"
					},
					"organisation_id": {
						"description": "Organisation UUID scope",
						"type": "string"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_deployment",
			"description": "Enterprise-gated. Update a registered Gateway deployment, its workspace or JWT-sub access, or rotate its authentication secret. Use get_deployment first to inspect current settings. Rotation returns the new secret once in this MCP transcript, so store it securely immediately. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"minLength": 1,
						"description": "Deployment UUID"
					},
					"name": {
						"description": "Replacement display name",
						"type": "string"
					},
					"type": {
						"description": "Replacement deployment type",
						"type": "string",
						"enum": ["production", "non_production"]
					},
					"status": {
						"description": "Replacement deployment status",
						"type": "string",
						"enum": ["active", "archived"]
					},
					"deployment_config": {
						"description": "Replacement Gateway configuration, or null to clear it",
						"anyOf": [
							{
								"type": "object",
								"propertyNames": {
									"type": "string"
								},
								"additionalProperties": {}
							},
							{
								"type": "null"
							}
						]
					},
					"is_default": {
						"description": "Whether this is the default deployment",
						"type": "boolean"
					},
					"rotate_auth": {
						"description": "Rotate the one-time deployment authentication secret",
						"type": "boolean"
					},
					"override_existing": {
						"description": "Allow replacement of existing deployment settings",
						"type": "boolean"
					},
					"gateway_base_url": {
						"description": "Self-hosted Gateway base URL",
						"type": "string",
						"format": "uri"
					},
					"mcp_gateway_base_url": {
						"description": "MCP Gateway base URL",
						"type": "string",
						"format": "uri"
					},
					"is_dataservice_hosted": {
						"description": "Whether the deployment hosts its own data service",
						"anyOf": [
							{
								"type": "number",
								"const": 0
							},
							{
								"type": "number",
								"const": 1
							}
						]
					},
					"is_playground_proxy_allowed": {
						"description": "Whether Playground proxy traffic is allowed",
						"anyOf": [
							{
								"type": "number",
								"const": 0
							},
							{
								"type": "number",
								"const": 1
							}
						]
					},
					"workspaces_allowed": {
						"description": "Workspace slugs this deployment may serve; empty allows all",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"jwt_subs_allowed": {
						"description": "JWT subject values allowed to use the deployment",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"jwt_sub_workspace_mapping": {
						"description": "JWT subject to workspace-slug mapping",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {
							"type": "string"
						}
					},
					"allow_all_workspaces": {
						"description": "Clear workspace restrictions and allow every workspace",
						"type": "boolean"
					},
					"remove_workspaces_allowed": {
						"description": "Workspace slugs to remove from the allowlist",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"remove_subs_allowed": {
						"description": "JWT subject values to remove from the allowlist",
						"type": "array",
						"items": {
							"type": "string"
						}
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "archive_deployment",
			"description": "Enterprise-gated. Archive a registered Gateway deployment by UUID. Use get_deployment first to confirm the target. Portkey soft-deletes the record; this stops treating it as active but does not permanently remove its history. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"minLength": 1,
						"description": "Deployment UUID to archive"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_virtual_keys",
			"description": "List provider API keys stored as virtual keys in your Portkey org. Use this to find slugs before wiring prompts/configs or auditing limits. Returns total plus name, slug, status, usage limits, rate limits, reset state, and model config.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Number of results per page (max 100)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "create_virtual_key",
			"description": "Store a provider API key as a virtual key. The raw key is encrypted and only returned at creation time, so save the returned slug and use it in prompts/configs. Optional usage and rate limits apply immediately, and the tool returns the new slug.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"name": {
						"type": "string",
						"description": "Display name for the virtual key"
					},
					"provider": {
						"type": "string",
						"description": "Provider slug (e.g., 'openai', 'anthropic', 'azure-openai', 'google')"
					},
					"key": {
						"description": "Provider API key; omit when secret_mappings supplies key",
						"type": "string"
					},
					"note": {
						"description": "Optional note or description for this key",
						"type": "string"
					},
					"workspace_id": {
						"description": "Workspace ID to create the key in",
						"type": "string"
					},
					"api_version": {
						"description": "API version (for Azure OpenAI)",
						"type": "string"
					},
					"resource_name": {
						"description": "Resource name (for Azure OpenAI)",
						"type": "string"
					},
					"deployment_name": {
						"description": "Deployment name (for Azure OpenAI)",
						"type": "string"
					},
					"deployment_configurations": {
						"description": "Azure deployment configurations with API versions and aliases",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"api_version": {
									"type": "string",
									"minLength": 1,
									"description": "Azure OpenAI API version"
								},
								"deployment_name": {
									"type": "string",
									"minLength": 1,
									"description": "Azure deployment name"
								},
								"alias": {
									"description": "Optional model alias for the deployment",
									"type": "string"
								},
								"is_default": {
									"description": "Whether this is the default Azure deployment",
									"type": "boolean"
								}
							},
							"required": ["api_version", "deployment_name"]
						}
					},
					"expires_at": {
						"description": "Expiration in ISO 8601 format",
						"type": "string",
						"format": "date-time",
						"pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z|([+-](?:[01]\\d|2[0-3]):[0-5]\\d)))$"
					},
					"secret_mappings": {
						"description": "Secret Reference mappings for key or model_config fields",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"target_field": {
									"type": "string",
									"description": "Virtual Key field populated from the Secret Reference"
								},
								"secret_reference_id": {
									"type": "string",
									"description": "Secret Reference ID that owns the external secret"
								},
								"secret_key": {
									"description": "Optional key selected from a structured external secret",
									"anyOf": [
										{
											"type": "string"
										},
										{
											"type": "null"
										}
									]
								},
								"value_format": {
									"description": "Whether Portkey reads the mapped value as JSON or a string",
									"anyOf": [
										{
											"type": "string",
											"enum": ["json", "string"]
										},
										{
											"type": "null"
										}
									]
								}
							},
							"required": ["target_field", "secret_reference_id"]
						}
					},
					"credit_limit": {
						"description": "Maximum usage cost threshold",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"alert_threshold": {
						"description": "Percentage of credit_limit at which to send alert emails (0-100)",
						"type": "number",
						"minimum": 0,
						"maximum": 100
					},
					"rate_limit_rpm": {
						"description": "Requests per minute limit",
						"type": "number",
						"exclusiveMinimum": 0
					}
				},
				"required": ["name", "provider"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "get_virtual_key",
			"description": "Fetch one virtual key by slug, including metadata, a masked secret, limits, status, and model config. Use this before updating or to inspect the current configuration.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"slug": {
						"type": "string",
						"description": "The unique slug identifier of the virtual key to retrieve"
					}
				},
				"required": ["slug"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_virtual_key",
			"description": "Update a virtual key's name, secret, note, or limits. Rotating the key takes effect immediately, and limit changes apply to downstream prompts and configs using this slug. Returns success when Portkey accepts the update.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"slug": {
						"type": "string",
						"description": "The slug of the virtual key to update"
					},
					"name": {
						"description": "New display name for the key",
						"type": "string"
					},
					"key": {
						"description": "New provider API key value",
						"type": "string"
					},
					"note": {
						"description": "New note or description",
						"type": "string"
					},
					"credit_limit": {
						"description": "New credit limit for usage",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"alert_threshold": {
						"description": "New alert threshold percentage (0-100)",
						"type": "number",
						"minimum": 0,
						"maximum": 100
					},
					"rate_limit_rpm": {
						"description": "New rate limit in requests per minute",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"deployment_configurations": {
						"description": "Replacement Azure deployment configurations",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"api_version": {
									"type": "string",
									"minLength": 1,
									"description": "Azure OpenAI API version"
								},
								"deployment_name": {
									"type": "string",
									"minLength": 1,
									"description": "Azure deployment name"
								},
								"alias": {
									"description": "Optional model alias for the deployment",
									"type": "string"
								},
								"is_default": {
									"description": "Whether this is the default Azure deployment",
									"type": "boolean"
								}
							},
							"required": ["api_version", "deployment_name"]
						}
					},
					"secret_mappings": {
						"description": "Replacement Secret Reference mappings",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"target_field": {
									"type": "string",
									"description": "Virtual Key field populated from the Secret Reference"
								},
								"secret_reference_id": {
									"type": "string",
									"description": "Secret Reference ID that owns the external secret"
								},
								"secret_key": {
									"description": "Optional key selected from a structured external secret",
									"anyOf": [
										{
											"type": "string"
										},
										{
											"type": "null"
										}
									]
								},
								"value_format": {
									"description": "Whether Portkey reads the mapped value as JSON or a string",
									"anyOf": [
										{
											"type": "string",
											"enum": ["json", "string"]
										},
										{
											"type": "null"
										}
									]
								}
							},
							"required": ["target_field", "secret_reference_id"]
						}
					}
				},
				"required": ["slug"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "delete_virtual_key",
			"description": "Delete a virtual key by slug. This is irreversible and will break prompts and configs that reference the slug, so confirm no active dependencies first. Returns success after removal.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"slug": {
						"type": "string",
						"description": "The slug of the virtual key to delete"
					}
				},
				"required": ["slug"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "create_api_key",
			"description": "Create a Portkey API key for auth. Org keys grant broader access; workspace keys are scoped. WARNING: The key secret is returned ONCE in the tool result and will be visible in MCP transcripts and LLM context — store it securely immediately. Using the key grants access immediately according to its scopes, defaults, and limits. Workspace keys require workspace_id and user keys require user_id.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"type": {
						"type": "string",
						"enum": ["organisation", "workspace"],
						"description": "Key type: 'organisation' for org-wide access or 'workspace' for workspace-scoped"
					},
					"sub_type": {
						"type": "string",
						"enum": ["user", "service"],
						"description": "Sub-type: 'user' for user-associated keys or 'service' for service accounts"
					},
					"name": {
						"type": "string",
						"description": "Display name for the API key"
					},
					"description": {
						"description": "Optional description for the key",
						"type": "string"
					},
					"organisation_id": {
						"description": "Organisation UUID",
						"type": "string"
					},
					"workspace_id": {
						"description": "Workspace ID (required for workspace-type keys)",
						"type": "string"
					},
					"user_id": {
						"description": "User ID (required for user sub-type keys)",
						"type": "string"
					},
					"scopes": {
						"type": "array",
						"items": {
							"type": "string"
						},
						"description": "Permission scopes for the key (e.g., ['logs.read', 'analytics.read'])"
					},
					"credit_limit": {
						"description": "Credit limit for usage",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"alert_threshold": {
						"description": "Alert threshold percentage (0-100)",
						"type": "number",
						"minimum": 0,
						"maximum": 100
					},
					"rate_limit_rpm": {
						"description": "Rate limit in requests per minute",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"rate_limits": {
						"description": "Request or token rate limits, or null to clear them",
						"anyOf": [
							{
								"type": "array",
								"items": {
									"type": "object",
									"properties": {
										"type": {
											"type": "string",
											"enum": ["requests", "tokens"],
											"description": "Whether the limit counts requests or tokens"
										},
										"unit": {
											"type": "string",
											"enum": ["rpd", "rph", "rpm", "rps", "rpw"],
											"description": "Rate window unit for the request or token count"
										},
										"value": {
											"type": "integer",
											"minimum": 0,
											"maximum": 9007199254740991,
											"description": "Maximum count in the selected rate window"
										}
									},
									"required": ["type", "unit", "value"]
								}
							},
							{
								"type": "null"
							}
						]
					},
					"default_config_id": {
						"description": "Default configuration ID to use with this key",
						"type": "string"
					},
					"default_metadata": {
						"description": "Default metadata key-value pairs",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {
							"type": "string"
						}
					},
					"default_allow_config_override": {
						"description": "Allow callers to override the default config",
						"type": "boolean"
					},
					"alert_emails": {
						"description": "Email addresses for alerts",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"expires_at": {
						"description": "Expiration date in ISO 8601 format",
						"type": "string",
						"format": "date-time",
						"pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z|([+-](?:[01]\\d|2[0-3]):[0-5]\\d)))$"
					},
					"rotation_policy": {
						"description": "Automatic API-key rotation policy, or null to disable it",
						"anyOf": [
							{
								"type": "object",
								"properties": {
									"rotation_period": {
										"description": "Built-in weekly or monthly automatic rotation cadence",
										"anyOf": [
											{
												"type": "string",
												"enum": ["weekly", "monthly"]
											},
											{
												"type": "null"
											}
										]
									},
									"next_rotation_at": {
										"description": "Explicit next rotation timestamp in ISO 8601 format",
										"anyOf": [
											{
												"type": "string",
												"format": "date-time",
												"pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z|([+-](?:[01]\\d|2[0-3]):[0-5]\\d)))$"
											},
											{
												"type": "null"
											}
										]
									},
									"key_transition_period_ms": {
										"description": "Overlap in milliseconds before the previous key expires",
										"type": "integer",
										"minimum": 1800000,
										"maximum": 9007199254740991
									}
								},
								"description": "Automatic API-key rotation policy, or null to remove it"
							},
							{
								"type": "null"
							}
						]
					}
				},
				"required": ["type", "sub_type", "name", "scopes"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_api_keys",
			"description": "List Portkey API keys for auditing access, scopes, defaults, limits, and expiration. Use this for API keys only; use list_virtual_keys for provider keys. Returns total plus id, type, status, workspace/user scope, limits, defaults, alert emails, and creation mode.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"page_size": {
						"description": "Number of results per page (max 100)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					},
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 9007199254740991
					},
					"workspace_id": {
						"description": "Filter by workspace ID",
						"type": "string"
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_api_key",
			"description": "Fetch one API key by UUID without revealing the secret. Use this to inspect scopes, defaults, limits, expiration, and reset state before changing access.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"format": "uuid",
						"pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
						"description": "The UUID of the API key to retrieve"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_api_key",
			"description": "Update an API key's name, description, scopes, defaults, or limits, unlike delete_api_key which revokes it or create_api_key which issues a new one. Changes take effect immediately for downstream callers, type and sub-type stay fixed after creation, and the call returns success without rotating the secret.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"format": "uuid",
						"pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
						"description": "The UUID of the API key to update"
					},
					"name": {
						"description": "New display name for the key",
						"type": "string"
					},
					"description": {
						"description": "New description for the key",
						"type": "string"
					},
					"scopes": {
						"description": "New permission scopes for the key",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"credit_limit": {
						"description": "New credit limit for usage",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"alert_threshold": {
						"description": "New alert threshold percentage (0-100)",
						"type": "number",
						"minimum": 0,
						"maximum": 100
					},
					"rate_limit_rpm": {
						"description": "New rate limit in requests per minute",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"rate_limits": {
						"description": "Replacement request or token rate limits, or null to clear",
						"anyOf": [
							{
								"type": "array",
								"items": {
									"type": "object",
									"properties": {
										"type": {
											"type": "string",
											"enum": ["requests", "tokens"],
											"description": "Whether the limit counts requests or tokens"
										},
										"unit": {
											"type": "string",
											"enum": ["rpd", "rph", "rpm", "rps", "rpw"],
											"description": "Rate window unit for the request or token count"
										},
										"value": {
											"type": "integer",
											"minimum": 0,
											"maximum": 9007199254740991,
											"description": "Maximum count in the selected rate window"
										}
									},
									"required": ["type", "unit", "value"]
								}
							},
							{
								"type": "null"
							}
						]
					},
					"reset_usage": {
						"description": "Set to a nonnegative acknowledgement value to reset usage",
						"type": "number",
						"minimum": 0
					},
					"default_config_id": {
						"description": "New default configuration ID",
						"type": "string"
					},
					"default_metadata": {
						"description": "New default metadata key-value pairs",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {
							"type": "string"
						}
					},
					"default_allow_config_override": {
						"description": "Whether callers may override the key's default config",
						"type": "boolean"
					},
					"alert_emails": {
						"description": "New email addresses for alerts",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"expires_at": {
						"description": "New expiration date in ISO 8601 format, or null to remove expiration",
						"anyOf": [
							{
								"type": "string",
								"format": "date-time",
								"pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z|([+-](?:[01]\\d|2[0-3]):[0-5]\\d)))$"
							},
							{
								"type": "null"
							}
						]
					},
					"rotation_policy": {
						"description": "Replacement rotation policy, or null to disable it",
						"anyOf": [
							{
								"type": "object",
								"properties": {
									"rotation_period": {
										"description": "Built-in weekly or monthly automatic rotation cadence",
										"anyOf": [
											{
												"type": "string",
												"enum": ["weekly", "monthly"]
											},
											{
												"type": "null"
											}
										]
									},
									"next_rotation_at": {
										"description": "Explicit next rotation timestamp in ISO 8601 format",
										"anyOf": [
											{
												"type": "string",
												"format": "date-time",
												"pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z|([+-](?:[01]\\d|2[0-3]):[0-5]\\d)))$"
											},
											{
												"type": "null"
											}
										]
									},
									"key_transition_period_ms": {
										"description": "Overlap in milliseconds before the previous key expires",
										"type": "integer",
										"minimum": 1800000,
										"maximum": 9007199254740991
									}
								},
								"description": "Automatic API-key rotation policy, or null to remove it"
							},
							{
								"type": "null"
							}
						]
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "delete_api_key",
			"description": "Delete an API key by UUID. This cannot be undone, revokes access immediately, and can break active sessions using the key. Returns success after revocation.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"format": "uuid",
						"pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
						"description": "The UUID of the API key to delete"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "rotate_api_key",
			"description": "Rotate an API key without changing its identity or scopes. The new secret is returned once and exposed to this MCP transcript, while the previous secret remains valid until key_transition_expires_at. Store the new key securely, update callers during the transition window, and never log either secret.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"format": "uuid",
						"pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
						"description": "API key UUID obtained from list_api_keys"
					},
					"key_transition_period_ms": {
						"description": "Overlap in milliseconds while the previous key remains valid; minimum 1,800,000 (30 minutes), for example 3,600,000 for 1 hour",
						"type": "integer",
						"minimum": 1800000,
						"maximum": 9007199254740991
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Rotate API Key",
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_collections",
			"description": "List prompt collections in the workspace, optionally filtering by name or workspace. Returns ids, names, slugs, and timestamps so you can choose a collection_id before create_prompt, get_collection, or list_prompts.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"workspace_id": {
						"description": "Filter by workspace ID",
						"type": "string"
					},
					"search": {
						"description": "Search collections by name",
						"type": "string"
					},
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Results per page (max 100)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "create_collection",
			"description": "Create a new prompt collection for organizing prompts by app. Use this when you need a new namespace before create_prompt; returns the collection id and slug, and does not move any prompts.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"name": {
						"type": "string",
						"description": "Collection name (e.g., 'hourlink', 'apizone', 'research-pilot')"
					},
					"workspace_id": {
						"description": "Workspace ID to create collection in",
						"type": "string"
					}
				},
				"required": ["name"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "get_collection",
			"description": "Fetch one collection by id or slug and return its name, slug, workspace, and timestamps. Use list_collections when browsing and get_collection when you already know the target.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"collection_id": {
						"type": "string",
						"description": "Collection ID or slug to retrieve"
					}
				},
				"required": ["collection_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_collection",
			"description": "Update a collection's name or description only. This does not move prompts or change membership, so use it for metadata changes rather than reorganization.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"collection_id": {
						"type": "string",
						"description": "Collection ID to update"
					},
					"name": {
						"description": "New name for the collection",
						"type": "string"
					},
					"description": {
						"description": "New description for the collection",
						"type": "string"
					}
				},
				"required": ["collection_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "delete_collection",
			"description": "Delete a prompt collection by ID. This cannot be undone; prompts stay in the workspace but lose their collection grouping, so reassign them first if organization matters.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"collection_id": {
						"type": "string",
						"description": "Collection ID to delete"
					}
				},
				"required": ["collection_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "create_prompt",
			"description": "Create a new prompt template and initial version. Use this for first-time setup; use migrate_prompt for idempotent CI/CD flows. Accepts plain text or structured chat messages, creates a new version immediately, and returns the prompt id, slug, and version id. For multi-message chat prompts pass messages (preferred) or a JSON-encoded array as string.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"name": {
						"type": "string",
						"description": "Display name for the prompt"
					},
					"collection_id": {
						"type": "string",
						"description": "Collection ID to organize the prompt in (use list_collections to find)"
					},
					"string": {
						"description": "Legacy prompt template string. Use plain text for single-message prompts, or a JSON-encoded messages array string for multi-message chat prompts.",
						"type": "string"
					},
					"messages": {
						"description": "Structured chat template alias. Serialized to the legacy string format before creation.",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"role": {
									"type": "string",
									"enum": ["system", "user", "assistant"],
									"description": "Message role in the chat template"
								},
								"content": {
									"type": "array",
									"items": {
										"type": "object",
										"properties": {
											"type": {
												"type": "string",
												"description": "Content block type"
											},
											"text": {
												"description": "Text content for text-based blocks",
												"type": "string"
											}
										},
										"required": ["type"],
										"additionalProperties": {},
										"description": "Content block within a structured chat message"
									},
									"description": "Message content blocks"
								}
							},
							"required": ["role", "content"],
							"additionalProperties": {},
							"description": "Structured chat message in a prompt template"
						}
					},
					"parameters": {
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {},
						"description": "Default values for template variables"
					},
					"virtual_key": {
						"type": "string",
						"description": "Virtual key slug for model access"
					},
					"model": {
						"description": "Model identifier (e.g., 'gpt-4', 'claude-3-opus'). Required unless ai_model_id or finetune_id is provided",
						"type": "string"
					},
					"ai_model_id": {
						"description": "AI model ID (alternative to model). Required unless model or finetune_id is provided",
						"type": "string"
					},
					"finetune_id": {
						"description": "Fine-tune ID (alternative to model). Required unless model or ai_model_id is provided",
						"type": "string"
					},
					"version_description": {
						"description": "Description for this prompt version",
						"type": "string"
					},
					"template_metadata": {
						"description": "Custom metadata (app, env, source_file, etc.)",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"functions": {
						"description": "Function definitions for function calling",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"name": {
									"type": "string",
									"description": "Function name"
								},
								"description": {
									"description": "Function description",
									"type": "string"
								},
								"parameters": {
									"description": "Function parameters schema",
									"type": "object",
									"propertyNames": {
										"type": "string"
									},
									"additionalProperties": {}
								}
							},
							"required": ["name"]
						}
					},
					"tools": {
						"description": "Tool definitions for tool use",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"type": {
									"type": "string",
									"const": "function",
									"description": "Tool type"
								},
								"function": {
									"type": "object",
									"properties": {
										"name": {
											"type": "string",
											"description": "Function name"
										},
										"description": {
											"description": "Function description",
											"type": "string"
										},
										"parameters": {
											"description": "Function parameters schema",
											"type": "object",
											"propertyNames": {
												"type": "string"
											},
											"additionalProperties": {}
										}
									},
									"required": ["name"],
									"description": "Function definition"
								}
							},
							"required": ["type", "function"]
						}
					},
					"tool_choice": {
						"description": "Tool choice strategy",
						"type": "object",
						"properties": {
							"mode": {
								"type": "string",
								"enum": ["auto", "none", "function"],
								"description": "Tool choice mode: auto lets the model decide, none disables tool use, function forces one function by name"
							},
							"function_name": {
								"description": "Required when mode is 'function'; ignored otherwise",
								"type": "string"
							}
						},
						"required": ["mode"]
					},
					"is_raw_template": {
						"description": "Render the stored template as a raw structural template",
						"type": "boolean"
					},
					"dry_run": {
						"description": "When true, validate without creating",
						"type": "boolean"
					}
				},
				"required": ["name", "collection_id", "parameters", "virtual_key"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_prompts",
			"description": "List prompts across the workspace, with optional collection, workspace, or search filters. Returns a paginated summary with id, name, slug, model, and status so you can choose a prompt_id before get_prompt, update_prompt, or render_prompt.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"collection_id": {
						"description": "Filter by collection ID (recommended for app-specific prompts)",
						"type": "string"
					},
					"workspace_id": {
						"description": "Filter by workspace ID",
						"type": "string"
					},
					"search": {
						"description": "Search prompts by name",
						"type": "string"
					},
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Results per page (max 100)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_prompt",
			"description": "Fetch a prompt's full definition, active version, and version history. Use this before updating, publishing, rendering, or copying a prompt when you need the stored template and metadata. For multi-message chat prompts pass messages (preferred) or a JSON-encoded array as string.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"prompt_id": {
						"type": "string",
						"description": "Prompt ID or slug to retrieve"
					}
				},
				"required": ["prompt_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_prompt",
			"description": "Update an existing prompt and create a new archived version. Only provided fields change, and publish_prompt is what makes the new version active. For multi-message chat prompts pass messages (preferred) or a JSON-encoded array as string.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"prompt_id": {
						"type": "string",
						"description": "Prompt ID or slug to update"
					},
					"name": {
						"description": "New display name for the prompt",
						"type": "string"
					},
					"collection_id": {
						"description": "Move to a different collection",
						"type": "string"
					},
					"string": {
						"description": "Legacy prompt template string. Use plain text for single-message prompts, or a JSON-encoded messages array string for multi-message chat prompts.",
						"type": "string"
					},
					"messages": {
						"description": "Structured chat template alias for updates. Serialized to the legacy string format before the prompt is updated.",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"role": {
									"type": "string",
									"enum": ["system", "user", "assistant"],
									"description": "Message role in the chat template"
								},
								"content": {
									"type": "array",
									"items": {
										"type": "object",
										"properties": {
											"type": {
												"type": "string",
												"description": "Content block type"
											},
											"text": {
												"description": "Text content for text-based blocks",
												"type": "string"
											}
										},
										"required": ["type"],
										"additionalProperties": {},
										"description": "Content block within a structured chat message"
									},
									"description": "Message content blocks"
								}
							},
							"required": ["role", "content"],
							"additionalProperties": {},
							"description": "Structured chat message in a prompt template"
						}
					},
					"parameters": {
						"description": "New default values for template variables",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"model": {
						"description": "New model identifier",
						"type": "string"
					},
					"virtual_key": {
						"description": "New virtual key slug",
						"type": "string"
					},
					"version_description": {
						"description": "Description for this version",
						"type": "string"
					},
					"template_metadata": {
						"description": "New metadata",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"functions": {
						"description": "New function definitions",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"name": {
									"type": "string",
									"description": "Function name"
								},
								"description": {
									"description": "Function description",
									"type": "string"
								},
								"parameters": {
									"description": "Function parameters schema",
									"type": "object",
									"propertyNames": {
										"type": "string"
									},
									"additionalProperties": {}
								}
							},
							"required": ["name"]
						}
					},
					"tools": {
						"description": "New tool definitions",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"type": {
									"type": "string",
									"const": "function",
									"description": "Tool type"
								},
								"function": {
									"type": "object",
									"properties": {
										"name": {
											"type": "string",
											"description": "Function name"
										},
										"description": {
											"description": "Function description",
											"type": "string"
										},
										"parameters": {
											"description": "Function parameters schema",
											"type": "object",
											"propertyNames": {
												"type": "string"
											},
											"additionalProperties": {}
										}
									},
									"required": ["name"],
									"description": "Function definition"
								}
							},
							"required": ["type", "function"]
						}
					},
					"tool_choice": {
						"description": "New tool choice strategy",
						"type": "object",
						"properties": {
							"mode": {
								"type": "string",
								"enum": ["auto", "none", "function"],
								"description": "Tool choice mode: auto lets the model decide, none disables tool use, function forces one function by name"
							},
							"function_name": {
								"description": "Required when mode is 'function'; ignored otherwise",
								"type": "string"
							}
						},
						"required": ["mode"]
					},
					"is_raw_template": {
						"description": "Enable or disable raw structural-template rendering",
						"type": "boolean"
					},
					"dry_run": {
						"description": "When true, validate without updating",
						"type": "boolean"
					}
				},
				"required": ["prompt_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "delete_prompt",
			"description": "Delete a prompt and all its versions by id. This cannot be undone, immediately breaks callers using the slug, and should only be used after checking list_prompt_versions or confirming you do not need an audit trail.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"prompt_id": {
						"type": "string",
						"description": "Prompt ID or slug to delete"
					}
				},
				"required": ["prompt_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "publish_prompt",
			"description": "Publish a specific version of a prompt as the active default, unlike promote_prompt which copies across environments or update_prompt which creates a new draft. This immediately routes all callers using the slug to that version and there is no rollback, so use list_prompt_versions to pick the version and update_prompt first if you need to create new content before promoting it.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"prompt_id": {
						"type": "string",
						"description": "Prompt ID or slug to publish"
					},
					"version": {
						"type": "number",
						"exclusiveMinimum": 0,
						"description": "Version number to publish as the default"
					}
				},
				"required": ["prompt_id", "version"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_prompt_versions",
			"description": "List all versions of one prompt, including version number, description, status, label, and a short template preview. Use this for history or to choose a version_id before publish_prompt or update_prompt_version.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"prompt_id": {
						"type": "string",
						"description": "Prompt ID or slug to list versions for"
					}
				},
				"required": ["prompt_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "render_prompt",
			"description": "Render a prompt by substituting variables and returning the final messages without calling the model. Use this to verify template output before a completion; run_prompt_completion is the tool that actually invokes the model.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"prompt_id": {
						"type": "string",
						"description": "Prompt ID or slug to render"
					},
					"variables": {
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {
							"anyOf": [
								{
									"type": "string"
								},
								{
									"type": "number"
								},
								{
									"type": "boolean"
								}
							]
						},
						"description": "Variable values to substitute into the template"
					},
					"hyperparameters": {
						"description": "Override default hyperparameters",
						"type": "object",
						"properties": {
							"max_tokens": {
								"description": "Maximum tokens to generate",
								"type": "number",
								"exclusiveMinimum": 0
							},
							"temperature": {
								"description": "Sampling temperature (0-2)",
								"type": "number",
								"minimum": 0,
								"maximum": 2
							},
							"top_p": {
								"description": "Top-p sampling (0-1)",
								"type": "number",
								"minimum": 0,
								"maximum": 1
							},
							"top_k": {
								"description": "Top-k sampling",
								"type": "number",
								"exclusiveMinimum": 0
							},
							"presence_penalty": {
								"description": "Presence penalty (-2 to 2)",
								"type": "number",
								"minimum": -2,
								"maximum": 2
							},
							"frequency_penalty": {
								"description": "Frequency penalty (-2 to 2)",
								"type": "number",
								"minimum": -2,
								"maximum": 2
							},
							"stop": {
								"description": "Stop sequences",
								"type": "array",
								"items": {
									"type": "string"
								}
							}
						}
					}
				},
				"required": ["prompt_id", "variables"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "run_prompt_completion",
			"description": "Execute a prompt against the configured model and return the completion. This makes a billable model call, so use render_prompt first when you want to check the template and validate_completion_metadata when billing fields are uncertain.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"prompt_id": {
						"type": "string",
						"description": "Prompt ID or slug to execute"
					},
					"variables": {
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {
							"anyOf": [
								{
									"type": "string"
								},
								{
									"type": "number"
								},
								{
									"type": "boolean"
								}
							]
						},
						"description": "Variable values to substitute into the template"
					},
					"metadata": {
						"type": "object",
						"properties": {
							"client_id": {
								"type": "string",
								"description": "Client ID for billing attribution (REQUIRED)"
							},
							"app": {
								"type": "string",
								"minLength": 1,
								"description": "App identifier (REQUIRED). Use your deployed app name, for example 'hourlink' or 'support-console'."
							},
							"env": {
								"type": "string",
								"minLength": 1,
								"description": "Environment identifier (REQUIRED). Use your environment name, for example 'dev', 'staging', 'prod', or 'qa'."
							},
							"project_id": {
								"description": "Project ID for granular billing",
								"type": "string"
							},
							"feature": {
								"description": "Feature name for tracking",
								"type": "string"
							}
						},
						"required": ["client_id", "app", "env"],
						"description": "Billing metadata - client_id, app, env are REQUIRED for cost attribution"
					},
					"hyperparameters": {
						"description": "Override default hyperparameters",
						"type": "object",
						"properties": {
							"max_tokens": {
								"description": "Maximum tokens to generate",
								"type": "number",
								"exclusiveMinimum": 0
							},
							"temperature": {
								"description": "Sampling temperature (0-2)",
								"type": "number",
								"minimum": 0,
								"maximum": 2
							},
							"top_p": {
								"description": "Top-p sampling (0-1)",
								"type": "number",
								"minimum": 0,
								"maximum": 1
							},
							"top_k": {
								"description": "Top-k sampling",
								"type": "number",
								"exclusiveMinimum": 0
							},
							"presence_penalty": {
								"description": "Presence penalty (-2 to 2)",
								"type": "number",
								"minimum": -2,
								"maximum": 2
							},
							"frequency_penalty": {
								"description": "Frequency penalty (-2 to 2)",
								"type": "number",
								"minimum": -2,
								"maximum": 2
							},
							"stop": {
								"description": "Stop sequences",
								"type": "array",
								"items": {
									"type": "string"
								}
							}
						}
					}
				},
				"required": ["prompt_id", "variables", "metadata"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "migrate_prompt",
			"description": "Create or update a prompt in one idempotent step for CI/CD and prompt-as-code flows, unlike create_prompt which always makes a new prompt. Looks up the prompt by name within collection_id: if missing it creates the prompt, if found with different content it adds a new version (nothing is overwritten), and if the content already matches it no-ops; dry_run reports what would happen without changing anything. Stores app/env in template_metadata; get collection_id from list_collections. Returns the action taken (created, updated, or unchanged), dry_run flag, message, prompt id, slug, and version id.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"name": {
						"type": "string",
						"description": "Prompt name to create or find for update"
					},
					"app": {
						"type": "string",
						"minLength": 1,
						"description": "App identifier (REQUIRED). Use your deployed app name, for example 'hourlink' or 'support-console'."
					},
					"env": {
						"type": "string",
						"minLength": 1,
						"description": "Environment identifier (REQUIRED). Use your environment name, for example 'dev', 'staging', 'prod', or 'qa'."
					},
					"collection_id": {
						"type": "string",
						"description": "Collection ID to search in and create under"
					},
					"string": {
						"description": "Legacy prompt template string with {{variable}} mustache syntax.",
						"type": "string"
					},
					"messages": {
						"description": "Structured chat template alias for migrations. Serialized to the legacy string format before the prompt is created or updated.",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"role": {
									"type": "string",
									"enum": ["system", "user", "assistant"],
									"description": "Message role in the chat template"
								},
								"content": {
									"type": "array",
									"items": {
										"type": "object",
										"properties": {
											"type": {
												"type": "string",
												"description": "Content block type"
											},
											"text": {
												"description": "Text content for text-based blocks",
												"type": "string"
											}
										},
										"required": ["type"],
										"additionalProperties": {},
										"description": "Content block within a structured chat message"
									},
									"description": "Message content blocks"
								}
							},
							"required": ["role", "content"],
							"additionalProperties": {},
							"description": "Structured chat message in a prompt template"
						}
					},
					"parameters": {
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {},
						"description": "Default values for template variables"
					},
					"virtual_key": {
						"type": "string",
						"description": "Virtual key slug for model access"
					},
					"model": {
						"description": "Model identifier",
						"type": "string"
					},
					"version_description": {
						"description": "Description for this version",
						"type": "string"
					},
					"template_metadata": {
						"description": "Additional custom metadata",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"functions": {
						"description": "Function definitions",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"name": {
									"type": "string",
									"description": "Function name"
								},
								"description": {
									"description": "Function description",
									"type": "string"
								},
								"parameters": {
									"description": "Function parameters schema",
									"type": "object",
									"propertyNames": {
										"type": "string"
									},
									"additionalProperties": {}
								}
							},
							"required": ["name"]
						}
					},
					"tools": {
						"description": "Tool definitions",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"type": {
									"type": "string",
									"const": "function",
									"description": "Tool type"
								},
								"function": {
									"type": "object",
									"properties": {
										"name": {
											"type": "string",
											"description": "Function name"
										},
										"description": {
											"description": "Function description",
											"type": "string"
										},
										"parameters": {
											"description": "Function parameters schema",
											"type": "object",
											"propertyNames": {
												"type": "string"
											},
											"additionalProperties": {}
										}
									},
									"required": ["name"],
									"description": "Function definition"
								}
							},
							"required": ["type", "function"]
						}
					},
					"tool_choice": {
						"description": "Tool choice strategy",
						"type": "object",
						"properties": {
							"mode": {
								"type": "string",
								"enum": ["auto", "none", "function"],
								"description": "Tool choice mode: auto lets the model decide, none disables tool use, function forces one function by name"
							},
							"function_name": {
								"description": "Required when mode is 'function'; ignored otherwise",
								"type": "string"
							}
						},
						"required": ["mode"]
					},
					"is_raw_template": {
						"description": "Preserve raw structural-template rendering semantics",
						"type": "boolean"
					},
					"dry_run": {
						"description": "When true, only check what action would be taken without making changes",
						"type": "boolean"
					}
				},
				"required": [
					"name",
					"app",
					"env",
					"collection_id",
					"parameters",
					"virtual_key"
				],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": false
			}
		},
		{
			"name": "promote_prompt",
			"description": "Copy a prompt from one environment to another and create or update the target automatically. Use this for staged releases when you want the target prompt synchronized without manual edits, and it returns both source and target version ids.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"source_prompt_id": {
						"type": "string",
						"description": "Source prompt ID or slug (e.g., staging prompt)"
					},
					"target_collection_id": {
						"type": "string",
						"description": "Target collection ID for the promoted prompt"
					},
					"target_name": {
						"description": "Target prompt name (defaults to source name with env suffix replaced)",
						"type": "string"
					},
					"target_env": {
						"type": "string",
						"minLength": 1,
						"description": "Environment identifier (REQUIRED). Use your environment name, for example 'dev', 'staging', 'prod', or 'qa'."
					},
					"virtual_key": {
						"description": "Virtual key ID to use (defaults to source prompt's virtual_key)",
						"type": "string"
					}
				},
				"required": ["source_prompt_id", "target_collection_id", "target_env"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "validate_completion_metadata",
			"description": "Preflight billing metadata before run_prompt_completion. Validates required fields and values without making changes, so you can catch attribution errors before paying for the call.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"client_id": {
						"description": "Client ID for billing attribution",
						"type": "string"
					},
					"app": {
						"type": "string",
						"minLength": 1,
						"description": "App identifier (REQUIRED). Use your deployed app name, for example 'hourlink' or 'support-console'."
					},
					"env": {
						"type": "string",
						"minLength": 1,
						"description": "Environment identifier (REQUIRED). Use your environment name, for example 'dev', 'staging', 'prod', or 'qa'."
					},
					"project_id": {
						"description": "Project ID for granular billing",
						"type": "string"
					},
					"feature": {
						"description": "Feature name for tracking",
						"type": "string"
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_prompt_version",
			"description": "Retrieve a specific prompt version by its version UUID. Use list_prompt_versions to find the id first; returns the template, parameters, and model config for that version.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"prompt_id": {
						"type": "string",
						"description": "Prompt ID or slug"
					},
					"version_id": {
						"type": "string",
						"description": "Version UUID to retrieve"
					}
				},
				"required": ["prompt_id", "version_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_prompt_version",
			"description": "Update a specific prompt version's label assignment. This only assigns or removes a label, and null clears the label after you look up ids with list_prompt_labels.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"prompt_id": {
						"type": "string",
						"description": "Prompt ID or slug"
					},
					"version_id": {
						"type": "string",
						"description": "Version UUID to update"
					},
					"label_id": {
						"anyOf": [
							{
								"type": "string"
							},
							{
								"type": "null"
							}
						],
						"description": "Label ID to assign to this version, or null to remove the label"
					}
				},
				"required": ["prompt_id", "version_id", "label_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "get_cost_analytics",
			"description": "Get cost time-series data with summary.total_cost, summary.average_cost_per_request, and per-bucket total/avg cost. Use this for spend analysis and spike detection; use get_token_analytics when you need token volume instead of monetary cost. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_request_analytics",
			"description": "Get request-volume time-series data with summary.total_requests, summary.successful_requests, summary.failed_requests, and per-bucket total/success/failed counts. Use this for traffic and reliability trends; use get_error_analytics when you only need error counts. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_token_analytics",
			"description": "Get token-usage time-series data with summary.total_tokens, summary.prompt_tokens, summary.completion_tokens, and per-bucket total/prompt/completion counts. Use this for consumption trends; use get_cost_analytics when you need spend instead of token volume. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_latency_analytics",
			"description": "Get latency time-series data with summary.avg_latency_ms, summary.p50_latency_ms, summary.p90_latency_ms, summary.p99_latency_ms, and per-bucket latency percentiles in ms. Use this to spot slowdowns and regressions; use get_cache_hit_latency when you only want cache-hit latency. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_error_analytics",
			"description": "Get error-count time-series data with summary.total_errors and per-bucket counts. Use this for high-level error trends; use get_error_rate_analytics for percentages, or get_error_status_codes_analytics and get_error_stacks_analytics for breakdowns. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_error_rate_analytics",
			"description": "Get error-rate time-series data with summary.error_rate_percent and per-bucket percentages of total requests. Use this for reliability and SLA trends; use get_error_analytics for absolute error counts instead. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_cache_hit_latency",
			"description": "Get cache-hit-only latency time-series data with summary.total_latency, summary.avg_latency, and per-bucket total/avg latency. Use this to evaluate cached-response speed; use get_latency_analytics for all requests. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_cache_hit_rate",
			"description": "Get cache-effectiveness time-series data with summary.hit_rate, summary.total_hits, summary.total_misses, and per-bucket hits/misses/rate. Use this to measure cache effectiveness; use get_cache_hit_latency for speed rather than hit/miss ratio. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_cache_summary",
			"description": "Enterprise-gated. Get cache summary metrics for one workspace and time range: hits, average cache-hit latency, total requests, and percentage speedup. Use the graph cache tools when you need changes over time instead of one aggregate. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"type": "string",
						"minLength": 1,
						"description": "Workspace slug to analyze"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					}
				},
				"required": [
					"time_of_generation_min",
					"time_of_generation_max",
					"workspace_slug"
				],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_users_analytics",
			"description": "Get user-growth time-series data with summary.total_active_users, summary.total_new_users, and per-bucket active/new user counts. Use this for growth and adoption trends; use get_user_requests_analytics for per-user traffic or get_analytics_group_users for per-user cost and token detail. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_error_stacks_analytics",
			"description": "Get stacked error-series data grouped by HTTP status code over time, with summary and per-code series. Use this to see which error classes dominate; use get_error_status_codes_analytics for distinct-code distribution instead. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_error_status_codes_analytics",
			"description": "Get HTTP error-code distribution time-series data with summary and per-code series. Use this to see which codes occur most often; use get_error_stacks_analytics for stacked or cumulative breakdowns. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_user_requests_analytics",
			"description": "Get per-user request-count time-series data with counts grouped by user. Use this to find heavy users and traffic concentration; use get_users_analytics for aggregate active and new user trends instead. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_rescued_requests_analytics",
			"description": "Get rescued-request time-series data showing requests recovered by retry or fallback handling. Use this only when your configs include resilience features, and use it to measure how often recovery logic saved requests. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_feedback_analytics",
			"description": "Get feedback-submission time-series data with summary totals and per-bucket counts. Use this as the top-level feedback trend view; use get_feedback_models_analytics, get_feedback_scores_analytics, or get_feedback_weighted_analytics for breakdowns. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_feedback_models_analytics",
			"description": "Get feedback time-series data grouped by model, with per-model counts over time. Use this to compare feedback volume and satisfaction across models; use get_feedback_analytics for the overall total instead. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_feedback_scores_analytics",
			"description": "Get raw feedback-score distribution time-series data with per-score buckets. Use this to understand sentiment mix; use get_feedback_weighted_analytics for calibrated scores with weighting. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_feedback_weighted_analytics",
			"description": "Get weighted feedback-score time-series data using the weight recorded at feedback creation. Use this for calibrated quality metrics; use get_feedback_scores_analytics for the raw unweighted distribution. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_analytics_group_users",
			"description": "Get a paginated per-user breakdown with total_groups, group_count, and a users array containing request count, cost, and token usage. Use this for billing, audits, or top-consumer analysis; use get_users_analytics for aggregate active and new user trends. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					},
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"minimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Results per page (max 100)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_analytics_group_models",
			"description": "Get a paginated per-model breakdown with total_groups, group_count, and a models array containing request count, cost, and token usage. Use this to compare model cost, popularity, and efficiency; use get_token_analytics or get_cost_analytics for time-series trends instead. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					},
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"minimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Results per page (max 100)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					}
				},
				"required": ["time_of_generation_min", "time_of_generation_max"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_analytics_group_providers",
			"description": "Enterprise-gated. Get provider-grouped analytics for one workspace and time range with selectable metrics, ordering, pagination, and optional total count. Use this when comparing provider traffic or reliability; requested metric fields are preserved in each provider row. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"type": "string",
						"minLength": 1,
						"description": "Workspace slug to analyze"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					},
					"current_page": {
						"description": "Zero-based result page",
						"type": "integer",
						"minimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Number of provider groups per page",
						"type": "integer",
						"minimum": 0,
						"maximum": 9007199254740991
					},
					"order_by": {
						"description": "Metric or field used to order groups",
						"type": "string"
					},
					"order_by_type": {
						"description": "Sort direction accepted by Portkey, such as asc or desc",
						"type": "string"
					},
					"columns": {
						"description": "Provider metrics to include in each grouped row",
						"type": "array",
						"items": {
							"type": "string",
							"enum": [
								"requests",
								"cost",
								"total_tokens",
								"avg_tokens",
								"avg_input_tokens",
								"avg_output_tokens",
								"avg_latency",
								"p95_latency",
								"p99_latency",
								"success_rate",
								"error_count",
								"cache_hit_rate",
								"last_seen",
								"first_seen"
							]
						}
					},
					"include_total": {
						"description": "Request the total provider-group count",
						"type": "boolean"
					}
				},
				"required": [
					"time_of_generation_min",
					"time_of_generation_max",
					"workspace_slug"
				],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_analytics_group_metadata",
			"description": "Get a paginated metadata breakdown with total_groups, group_count, and a metadata_groups array grouped by the required metadata_key. Use this for custom breakdowns like per-environment or per-feature analysis; pass metadata_key in addition to the time window. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"time_of_generation_min": {
						"type": "string",
						"description": "Start time for the analytics period (ISO8601 format, e.g., '2024-01-01T00:00:00Z')"
					},
					"time_of_generation_max": {
						"type": "string",
						"description": "End time for the analytics period (ISO8601 format, e.g., '2024-02-01T00:00:00Z')"
					},
					"total_units_min": {
						"description": "Minimum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"total_units_max": {
						"description": "Maximum number of total tokens to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_min": {
						"description": "Minimum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"cost_max": {
						"description": "Maximum cost in cents to filter by",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_min": {
						"description": "Minimum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"prompt_token_max": {
						"description": "Maximum number of prompt tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_min": {
						"description": "Minimum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"completion_token_max": {
						"description": "Maximum number of completion tokens",
						"type": "number",
						"minimum": 0
					},
					"status_code": {
						"description": "Legacy Portkey query param for HTTP status codes. Comma-separated string; prefer status_codes for structured inputs.",
						"type": "string"
					},
					"weighted_feedback_min": {
						"description": "Minimum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"weighted_feedback_max": {
						"description": "Maximum weighted feedback score (-10 to 10)",
						"type": "number",
						"minimum": -10,
						"maximum": 10
					},
					"virtual_keys": {
						"description": "Legacy Portkey query param for virtual key slugs. Comma-separated string; prefer virtual_key_slugs for structured inputs.",
						"type": "string"
					},
					"configs": {
						"description": "Legacy Portkey query param for config slugs. Comma-separated string; prefer config_slugs for structured inputs.",
						"type": "string"
					},
					"status_codes": {
						"description": "Structured alias for status_code. Use an array of HTTP status codes; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"virtual_key_slugs": {
						"description": "Structured alias for virtual_keys. Use an array of virtual key slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"config_slugs": {
						"description": "Structured alias for configs. Use an array of config slugs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"workspace_slug": {
						"description": "Filter by specific workspace",
						"type": "string"
					},
					"api_key_ids": {
						"description": "Legacy Portkey query param for API key UUIDs. Comma-separated string; request_analytics also accepts an array and normalizes it to this form.",
						"type": "string"
					},
					"metadata": {
						"description": "Legacy Portkey query param for metadata filtering. Stringified JSON object, e.g. '{\"env\":\"prod\",\"app\":\"myapp\"}'; prefer metadata_filter for structured inputs.",
						"type": "string"
					},
					"ai_org_model": {
						"description": "Legacy Portkey query param for provider/model pairs. Format: 'provider__model' with double underscore, e.g. 'openai__gpt-4' or 'anthropic__claude-3-opus'. Comma-separated string; prefer provider_models for structured inputs.",
						"type": "string"
					},
					"provider_models": {
						"description": "Structured alias for ai_org_model. Use provider__model strings in an array; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"trace_id": {
						"description": "Legacy Portkey query param for trace IDs. Comma-separated string; prefer trace_ids for structured inputs.",
						"type": "string"
					},
					"trace_ids": {
						"description": "Structured alias for trace_id. Use an array of trace IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"span_id": {
						"description": "Legacy Portkey query param for span IDs. Comma-separated string; prefer span_ids for structured inputs.",
						"type": "string"
					},
					"span_ids": {
						"description": "Structured alias for span_id. Use an array of span IDs; normalized to the legacy comma-separated Portkey query param.",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"metadata_filter": {
						"description": "Structured alias for metadata. Use an object such as { env: 'prod' }; normalized to a JSON string before the request is sent.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"prompt_slug": {
						"description": "Filter by prompt slug",
						"type": "string"
					},
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"minimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Results per page (max 100)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					},
					"metadata_key": {
						"type": "string",
						"description": "The metadata key to group by (e.g., 'env', 'app', 'client_id')"
					}
				},
				"required": [
					"time_of_generation_min",
					"time_of_generation_max",
					"metadata_key"
				],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_organisation_defaults",
			"description": "Get the organisation-wide input and output guardrails that workspaces inherit by default. Use this before update_organisation_defaults or when auditing baseline enforcement; it does not include per-workspace exclusions, which are available from the directional exclusion list tools. Requires an organisation service API key with organisation_settings.read scope. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Get Organisation Guardrail Defaults",
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_organisation_defaults",
			"description": "Replace the organisation-wide default input and/or output guardrail lists inherited by workspaces. Only supplied directions change, but enforcement updates immediately across non-excluded workspaces; inspect get_organisation_defaults and the directional workspace exclusions first. Repeating the same lists is safe. Requires an organisation service API key with organisation_settings.update scope. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"input_guardrails": {
						"description": "Ordered guardrail IDs or slugs to enforce on model input",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"output_guardrails": {
						"description": "Ordered guardrail IDs or slugs to enforce on model output",
						"type": "array",
						"items": {
							"type": "string"
						}
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Update Organisation Guardrail Defaults",
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "list_input_guardrail_workspace_exclusions",
			"description": "List workspaces excluded from organisation-wide input guardrails for one organisation. Use this to audit exceptions or establish the current state before the matching update tool; it reads input exclusions only and does not return the organisation's default guardrail list. Requires an organisation service API key with organisation_exclusions.list scope. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"organisation_id": {
						"type": "string",
						"description": "Organisation ID whose workspace exclusions should be listed"
					}
				},
				"required": ["organisation_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "List Input Guardrail Workspace Exclusions",
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_input_guardrail_workspace_exclusions",
			"description": "Set workspace exclusions from organisation-wide input guardrails. Each entry excludes or restores one workspace; override_existing replaces prior states while the default merge behavior preserves unmentioned workspaces. Review the matching list tool first because enforcement changes immediately. Repeating the same states is safe. Requires an organisation service API key with organisation_exclusions.update scope. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"organisation_id": {
						"type": "string",
						"description": "Organisation ID whose workspace exclusions should be updated"
					},
					"workspaces": {
						"minItems": 1,
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"workspace_id": {
									"type": "string",
									"description": "Workspace ID whose exclusion state changes"
								},
								"excluded": {
									"type": "boolean",
									"description": "True to exclude this workspace; false to restore enforcement"
								}
							},
							"required": ["workspace_id", "excluded"]
						},
						"description": "Workspace exclusion states to apply"
					},
					"override_existing": {
						"description": "Replace existing exclusion states instead of merging changes",
						"type": "boolean"
					}
				},
				"required": ["organisation_id", "workspaces"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Update Input Guardrail Workspace Exclusions",
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "list_output_guardrail_workspace_exclusions",
			"description": "List workspaces excluded from organisation-wide output guardrails for one organisation. Use this to audit exceptions or establish the current state before the matching update tool; it reads output exclusions only and does not return the organisation's default guardrail list. Requires an organisation service API key with organisation_exclusions.list scope. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"organisation_id": {
						"type": "string",
						"description": "Organisation ID whose workspace exclusions should be listed"
					}
				},
				"required": ["organisation_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "List Output Guardrail Workspace Exclusions",
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_output_guardrail_workspace_exclusions",
			"description": "Set workspace exclusions from organisation-wide output guardrails. Each entry excludes or restores one workspace; override_existing replaces prior states while the default merge behavior preserves unmentioned workspaces. Review the matching list tool first because enforcement changes immediately. Repeating the same states is safe. Requires an organisation service API key with organisation_exclusions.update scope. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"organisation_id": {
						"type": "string",
						"description": "Organisation ID whose workspace exclusions should be updated"
					},
					"workspaces": {
						"minItems": 1,
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"workspace_id": {
									"type": "string",
									"description": "Workspace ID whose exclusion state changes"
								},
								"excluded": {
									"type": "boolean",
									"description": "True to exclude this workspace; false to restore enforcement"
								}
							},
							"required": ["workspace_id", "excluded"]
						},
						"description": "Workspace exclusion states to apply"
					},
					"override_existing": {
						"description": "Replace existing exclusion states instead of merging changes",
						"type": "boolean"
					}
				},
				"required": ["organisation_id", "workspaces"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Update Output Guardrail Workspace Exclusions",
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "list_guardrails",
			"description": "List guardrails in the org with id, slug, status, ownership, and optional workspace/org filters. Use this to find IDs and slugs before get_guardrail, update_guardrail, or delete_guardrail.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"workspace_id": {
						"description": "Filter guardrails by workspace ID",
						"type": "string"
					},
					"organisation_id": {
						"description": "Filter guardrails by organization ID",
						"type": "string"
					},
					"page_size": {
						"description": "Number of items per page (1-100, default: 100)",
						"type": "integer",
						"minimum": 1,
						"maximum": 100
					},
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"minimum": 0,
						"maximum": 9007199254740991
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_guardrail",
			"description": "Fetch one guardrail by id or slug with its full checks and actions; use list_guardrails to discover ids first. Use before update_guardrail or delete_guardrail when you need the exact enforcement policy, and returns the full check and action configuration alongside status and ownership.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"guardrail_id": {
						"type": "string",
						"description": "The guardrail UUID or slug (with guard_ prefix) to retrieve"
					}
				},
				"required": ["guardrail_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "create_guardrail",
			"description": "Create a guardrail with checks and actions for request filtering. Create it first, then reference it from configs; the new version becomes the policy anchor for downstream use.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"name": {
						"type": "string",
						"description": "Name of the guardrail"
					},
					"checks": {
						"minItems": 1,
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"id": {
									"type": "string",
									"description": "Check identifier (e.g., 'default.jwt', 'default.pii')"
								},
								"name": {
									"description": "Display name for the check",
									"type": "string"
								},
								"is_enabled": {
									"description": "Whether the check is enabled",
									"type": "boolean"
								},
								"parameters": {
									"description": "Check-specific configuration. Supported examples include requestParametersCheck and parameters.forwardHeaders; forwarded headers can expose sensitive values, so allow only the minimum required names.",
									"type": "object",
									"propertyNames": {
										"type": "string"
									},
									"additionalProperties": {}
								}
							},
							"required": ["id"]
						},
						"description": "Array of checks to apply (at least one required)"
					},
					"actions": {
						"type": "object",
						"properties": {
							"deny": {
								"description": "Whether to deny the request on check failure",
								"type": "boolean"
							},
							"async": {
								"description": "Whether to run checks asynchronously",
								"type": "boolean"
							},
							"on_success": {
								"description": "Feedback configuration for successful checks",
								"type": "object",
								"properties": {
									"value": {
										"description": "Feedback value",
										"type": "number"
									},
									"weight": {
										"description": "Feedback weight",
										"type": "number"
									},
									"metadata": {
										"description": "Additional metadata",
										"type": "object",
										"propertyNames": {
											"type": "string"
										},
										"additionalProperties": {}
									}
								}
							},
							"on_fail": {
								"description": "Feedback configuration for failed checks",
								"type": "object",
								"properties": {
									"value": {
										"description": "Feedback value",
										"type": "number"
									},
									"weight": {
										"description": "Feedback weight",
										"type": "number"
									},
									"metadata": {
										"description": "Additional metadata",
										"type": "object",
										"propertyNames": {
											"type": "string"
										},
										"additionalProperties": {}
									}
								}
							},
							"on_fail_action": {
								"description": "Simple action on failure (e.g., 'block')",
								"type": "string"
							},
							"message": {
								"description": "Message to return when guardrail triggers",
								"type": "string"
							}
						},
						"description": "Actions to take when guardrail checks pass or fail"
					},
					"workspace_id": {
						"description": "Workspace ID to create the guardrail in",
						"type": "string"
					},
					"organisation_id": {
						"description": "Organisation ID (required if workspace_id not provided)",
						"type": "string"
					}
				},
				"required": ["name", "checks", "actions"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "update_guardrail",
			"description": "Update a guardrail's name, checks, or actions, unlike create_guardrail which registers a new one or delete_guardrail which removes it. This creates a new version that takes effect immediately for dependent configs, so review list_guardrails first; returns the updated id, slug, and version_id.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"guardrail_id": {
						"type": "string",
						"description": "The guardrail UUID or slug to update"
					},
					"name": {
						"description": "New name for the guardrail",
						"type": "string"
					},
					"checks": {
						"description": "Updated array of checks to apply",
						"minItems": 1,
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"id": {
									"type": "string",
									"description": "Check identifier (e.g., 'default.jwt', 'default.pii')"
								},
								"name": {
									"description": "Display name for the check",
									"type": "string"
								},
								"is_enabled": {
									"description": "Whether the check is enabled",
									"type": "boolean"
								},
								"parameters": {
									"description": "Check-specific configuration. Supported examples include requestParametersCheck and parameters.forwardHeaders; forwarded headers can expose sensitive values, so allow only the minimum required names.",
									"type": "object",
									"propertyNames": {
										"type": "string"
									},
									"additionalProperties": {}
								}
							},
							"required": ["id"]
						}
					},
					"actions": {
						"description": "Updated actions configuration",
						"type": "object",
						"properties": {
							"deny": {
								"description": "Whether to deny the request on check failure",
								"type": "boolean"
							},
							"async": {
								"description": "Whether to run checks asynchronously",
								"type": "boolean"
							},
							"on_success": {
								"description": "Feedback configuration for successful checks",
								"type": "object",
								"properties": {
									"value": {
										"description": "Feedback value",
										"type": "number"
									},
									"weight": {
										"description": "Feedback weight",
										"type": "number"
									},
									"metadata": {
										"description": "Additional metadata",
										"type": "object",
										"propertyNames": {
											"type": "string"
										},
										"additionalProperties": {}
									}
								}
							},
							"on_fail": {
								"description": "Feedback configuration for failed checks",
								"type": "object",
								"properties": {
									"value": {
										"description": "Feedback value",
										"type": "number"
									},
									"weight": {
										"description": "Feedback weight",
										"type": "number"
									},
									"metadata": {
										"description": "Additional metadata",
										"type": "object",
										"propertyNames": {
											"type": "string"
										},
										"additionalProperties": {}
									}
								}
							},
							"on_fail_action": {
								"description": "Simple action on failure (e.g., 'block')",
								"type": "string"
							},
							"message": {
								"description": "Message to return when guardrail triggers",
								"type": "string"
							}
						}
					}
				},
				"required": ["guardrail_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "delete_guardrail",
			"description": "Delete a guardrail by id or slug. This is irreversible and removes the check from any configs that reference it, so review dependent configs first.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"guardrail_id": {
						"type": "string",
						"description": "The guardrail UUID or slug to delete"
					}
				},
				"required": ["guardrail_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_rate_limits",
			"description": "List rate-limit policies with their current condition, grouping, rate unit, target, status, and scope. Filter before retrieving or changing one policy, especially when archived policies should be included.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"workspace_id": {
						"description": "Filter by workspace UUID",
						"type": "string"
					},
					"status": {
						"description": "Filter by active or archived status",
						"type": "string",
						"enum": ["active", "archived"]
					},
					"type": {
						"description": "Filter by counter type",
						"type": "string",
						"enum": ["requests", "tokens"]
					},
					"unit": {
						"description": "Filter by reset interval",
						"type": "string",
						"enum": ["rpm", "rph", "rpd", "rpw"]
					},
					"target": {
						"description": "Filter by LLM or MCP-tool target",
						"type": "string",
						"enum": ["llm", "mcp_tools"]
					},
					"page_size": {
						"description": "Number of policies to return per page",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 9007199254740991
					},
					"current_page": {
						"description": "Zero-based page number",
						"type": "integer",
						"minimum": 0,
						"maximum": 9007199254740991
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_rate_limit",
			"description": "Get one rate-limit policy by id, including current conditions, grouping, unit, value, target, scope, and status. Pass archived status when retrieving a soft-deleted policy.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"minLength": 1,
						"description": "Rate-limit policy UUID"
					},
					"status": {
						"description": "Include an archived policy by status",
						"type": "string",
						"enum": ["active", "archived"]
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "create_rate_limit",
			"description": "Create a request or token rate-limit policy. Conditions and grouping must be non-empty; target llm uses model/provider keys, while target mcp_tools uses MCP server and tool keys.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"conditions": {
						"minItems": 1,
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"key": {
									"type": "string",
									"minLength": 1,
									"description": "Match key such as api_key, virtual_key, provider, config, prompt, model, endpoint_type, mcp_server, mcp_tool, workspace_id, or metadata.*"
								},
								"value": {
									"anyOf": [
										{
											"type": "string"
										},
										{
											"minItems": 1,
											"type": "array",
											"items": {
												"type": "string"
											}
										}
									],
									"description": "One value or several OR-matched values; use * for wildcard matching"
								},
								"excludes": {
									"description": "Values to exclude",
									"anyOf": [
										{
											"type": "string"
										},
										{
											"minItems": 1,
											"type": "array",
											"items": {
												"type": "string"
											}
										}
									]
								}
							},
							"required": ["key", "value"]
						},
						"description": "Policy match conditions"
					},
					"group_by": {
						"minItems": 1,
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"key": {
									"type": "string",
									"minLength": 1,
									"description": "Counter grouping key such as api_key, workspace_id, virtual_key, provider, config, prompt, model, mcp_server, mcp_tool, or metadata.*"
								}
							},
							"required": ["key"]
						},
						"description": "Counter grouping dimensions"
					},
					"type": {
						"type": "string",
						"enum": ["requests", "tokens"],
						"description": "Counter type"
					},
					"unit": {
						"type": "string",
						"enum": ["rpm", "rph", "rpd", "rpw"],
						"description": "Counter reset interval"
					},
					"value": {
						"type": "number",
						"minimum": 0,
						"description": "Maximum requests or tokens"
					},
					"name": {
						"description": "Optional policy display name",
						"type": "string"
					},
					"target": {
						"description": "LLM or MCP-tool enforcement target",
						"type": "string",
						"enum": ["llm", "mcp_tools"]
					},
					"workspace_id": {
						"description": "Workspace UUID scope",
						"type": "string"
					},
					"organisation_id": {
						"description": "Organisation UUID scope",
						"type": "string"
					}
				},
				"required": ["conditions", "group_by", "type", "unit", "value"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "update_rate_limit",
			"description": "Update a rate-limit policy's name, unit, value, or non-empty conditions by id. The public contract does not allow changing its type, target, or grouping after creation.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"minLength": 1,
						"description": "Rate-limit policy UUID"
					},
					"name": {
						"description": "Replacement display name",
						"type": "string"
					},
					"unit": {
						"description": "Replacement reset interval",
						"type": "string",
						"enum": ["rpm", "rph", "rpd", "rpw"]
					},
					"value": {
						"description": "Replacement maximum",
						"type": "number",
						"minimum": 0
					},
					"conditions": {
						"description": "Replacement conditions",
						"minItems": 1,
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"key": {
									"type": "string",
									"minLength": 1,
									"description": "Match key such as api_key, virtual_key, provider, config, prompt, model, endpoint_type, mcp_server, mcp_tool, workspace_id, or metadata.*"
								},
								"value": {
									"anyOf": [
										{
											"type": "string"
										},
										{
											"minItems": 1,
											"type": "array",
											"items": {
												"type": "string"
											}
										}
									],
									"description": "One value or several OR-matched values; use * for wildcard matching"
								},
								"excludes": {
									"description": "Values to exclude",
									"anyOf": [
										{
											"type": "string"
										},
										{
											"minItems": 1,
											"type": "array",
											"items": {
												"type": "string"
											}
										}
									]
								}
							},
							"required": ["key", "value"]
						}
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "delete_rate_limit",
			"description": "Delete a rate-limit policy by id. Portkey removes the active throttling policy immediately, so inspect dependent callers and the full policy before deleting it.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"minLength": 1,
						"description": "Rate-limit policy UUID to delete"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_usage_limits",
			"description": "List cumulative cost or token usage-limit policies with current conditions, grouping, reset schedule, status, and scope. Filter by workspace, policy type, status, and pagination.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"workspace_id": {
						"description": "Filter by workspace UUID",
						"type": "string"
					},
					"status": {
						"description": "Filter by active or archived status",
						"type": "string",
						"enum": ["active", "archived"]
					},
					"type": {
						"description": "Filter by budget type",
						"type": "string",
						"enum": ["cost", "tokens"]
					},
					"page_size": {
						"description": "Number of policies to return per page",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 9007199254740991
					},
					"current_page": {
						"description": "Zero-based page number",
						"type": "integer",
						"minimum": 0,
						"maximum": 9007199254740991
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_usage_limit",
			"description": "Get one cumulative usage-limit policy by id. Use list_usage_limits first when the id is unknown. Optionally include per-value usage counters and retrieve archived policies; scheduled reset timestamps are returned when present.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"minLength": 1,
						"description": "Usage-limit policy UUID"
					},
					"status": {
						"description": "Include an archived policy by status",
						"type": "string",
						"enum": ["active", "archived"]
					},
					"include_usage": {
						"description": "Include per-value usage counters",
						"type": "boolean"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "create_usage_limit",
			"description": "Create a cumulative cost or token usage-limit policy with non-empty conditions and grouping. A periodic reset can be weekly, monthly, or omitted for a cumulative lifetime limit.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"conditions": {
						"minItems": 1,
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"key": {
									"type": "string",
									"minLength": 1,
									"description": "Match key such as api_key, virtual_key, provider, config, prompt, model, endpoint_type, mcp_server, mcp_tool, workspace_id, or metadata.*"
								},
								"value": {
									"anyOf": [
										{
											"type": "string"
										},
										{
											"minItems": 1,
											"type": "array",
											"items": {
												"type": "string"
											}
										}
									],
									"description": "One value or several OR-matched values; use * for wildcard matching"
								},
								"excludes": {
									"description": "Values to exclude",
									"anyOf": [
										{
											"type": "string"
										},
										{
											"minItems": 1,
											"type": "array",
											"items": {
												"type": "string"
											}
										}
									]
								}
							},
							"required": ["key", "value"]
						},
						"description": "Policy match conditions"
					},
					"group_by": {
						"minItems": 1,
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"key": {
									"type": "string",
									"minLength": 1,
									"description": "Counter grouping key such as api_key, workspace_id, virtual_key, provider, config, prompt, model, mcp_server, mcp_tool, or metadata.*"
								}
							},
							"required": ["key"]
						},
						"description": "Usage grouping dimensions"
					},
					"type": {
						"type": "string",
						"enum": ["cost", "tokens"],
						"description": "Budget type"
					},
					"credit_limit": {
						"type": "number",
						"minimum": 0,
						"description": "Maximum cumulative usage"
					},
					"name": {
						"description": "Optional policy display name",
						"type": "string"
					},
					"alert_threshold": {
						"description": "Optional alert threshold",
						"anyOf": [
							{
								"type": "number",
								"minimum": 0
							},
							{
								"type": "null"
							}
						]
					},
					"periodic_reset": {
						"description": "Optional reset schedule",
						"anyOf": [
							{
								"type": "string",
								"enum": ["monthly", "weekly"]
							},
							{
								"type": "null"
							}
						]
					},
					"workspace_id": {
						"description": "Workspace UUID scope",
						"type": "string"
					},
					"organisation_id": {
						"description": "Organisation UUID scope",
						"type": "string"
					}
				},
				"required": ["conditions", "group_by", "type", "credit_limit"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "update_usage_limit",
			"description": "Update a cumulative usage-limit policy's name, credit limit, alert threshold, reset schedule, or one grouped value's usage. Conditions and grouping aren't mutable in the public contract.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"minLength": 1,
						"description": "Usage-limit policy UUID"
					},
					"name": {
						"description": "Replacement display name",
						"type": "string"
					},
					"credit_limit": {
						"description": "Replacement cumulative maximum",
						"type": "number",
						"minimum": 0
					},
					"alert_threshold": {
						"description": "Replacement alert threshold",
						"anyOf": [
							{
								"type": "number",
								"minimum": 0
							},
							{
								"type": "null"
							}
						]
					},
					"periodic_reset": {
						"description": "Replacement reset schedule",
						"anyOf": [
							{
								"type": "string",
								"enum": ["monthly", "weekly"]
							},
							{
								"type": "null"
							}
						]
					},
					"reset_usage_for_value": {
						"description": "Grouped value whose usage should reset",
						"type": "string"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "delete_usage_limit",
			"description": "Archive a cumulative usage-limit policy by id. The policy stops enforcing its budget but its historical record remains available through archived-status reads.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"minLength": 1,
						"description": "Usage-limit policy UUID to archive"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_usage_limit_entities",
			"description": "List the values currently tracked by one usage-limit policy with each value key and current usage. Filter by active or exhausted state, search text, and pagination before resetting one counter.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"limit_id": {
						"type": "string",
						"minLength": 1,
						"description": "Usage-limit policy UUID"
					},
					"status": {
						"description": "Filter by enforcement state",
						"type": "string",
						"enum": ["active", "exhausted"]
					},
					"search": {
						"description": "Search tracked values",
						"type": "string"
					},
					"page_size": {
						"description": "Number of entities per page",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					},
					"current_page": {
						"description": "Zero-based page number",
						"type": "integer",
						"minimum": 0,
						"maximum": 9007199254740991
					}
				},
				"required": ["limit_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "reset_usage_limit_entity",
			"description": "Reset the current usage counter for one tracked usage-limit entity. This changes enforcement immediately for that exact policy and entity, so use the entity id returned by list_usage_limit_entities.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"limit_id": {
						"type": "string",
						"minLength": 1,
						"description": "Usage-limit policy UUID"
					},
					"entity_id": {
						"type": "string",
						"minLength": 1,
						"description": "Tracked entity UUID to reset"
					}
				},
				"required": ["limit_id", "entity_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_audit_logs",
			"description": "List audit log events for a Portkey workspace or organization. Returns paginated action-level records with actor, resource, metadata, and timestamps for compliance or incident review; use this instead of analytics when you need individual events, not aggregates. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"workspace_id": {
						"description": "Filter audit logs by workspace ID",
						"type": "string"
					},
					"actor_id": {
						"description": "Filter by the user ID who performed the action",
						"type": "string"
					},
					"action": {
						"description": "Filter by action type (e.g., 'create', 'update', 'delete', 'login')",
						"type": "string"
					},
					"resource_type": {
						"description": "Filter by resource type (e.g., 'user', 'workspace', 'config', 'virtual_key')",
						"type": "string"
					},
					"resource_id": {
						"description": "Filter by specific resource ID",
						"type": "string"
					},
					"start_time": {
						"description": "Start of time range filter (ISO 8601 format, e.g., '2024-01-01T00:00:00Z')",
						"type": "string"
					},
					"end_time": {
						"description": "End of time range filter (ISO 8601 format, e.g., '2024-01-31T23:59:59Z')",
						"type": "string"
					},
					"current_page": {
						"description": "Page number for pagination (starts at 1)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Number of results per page (max 100)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "create_prompt_label",
			"description": "Create a prompt label for tagging prompt versions such as production, staging, or experiment. Requires either organisation_id or workspace_id to set scope, returns the new label id, and does not assign it to any versions yet.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"name": {
						"type": "string",
						"description": "Name of the label"
					},
					"organisation_id": {
						"description": "Organisation ID to create the label in",
						"type": "string"
					},
					"workspace_id": {
						"description": "Workspace ID to create the label in",
						"type": "string"
					},
					"description": {
						"description": "Description of the label",
						"type": "string"
					},
					"color_code": {
						"description": "Hex format, e.g. '#FF5733'. Optional — omit for default color.",
						"type": "string",
						"pattern": "^#[0-9A-Fa-f]{6}$"
					}
				},
				"required": ["name"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_prompt_labels",
			"description": "List labels across the workspace or organisation, with optional search and scope filters. Returns ids, names, colors, status, and timestamps so you can choose a label_id before get_prompt_label or update_prompt_version.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"organisation_id": {
						"description": "Filter by organisation ID",
						"type": "string"
					},
					"workspace_id": {
						"description": "Filter by workspace ID",
						"type": "string"
					},
					"search": {
						"description": "Search labels by name",
						"type": "string"
					},
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Results per page (max 100)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_prompt_label",
			"description": "Fetch one label's full definition, including scope, color, and status. Use this when you already know the label_id; list_prompt_labels is better for browsing candidates.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"label_id": {
						"type": "string",
						"description": "Label ID to retrieve"
					},
					"organisation_id": {
						"description": "Organisation ID for filtering",
						"type": "string"
					},
					"workspace_id": {
						"description": "Workspace ID for filtering",
						"type": "string"
					}
				},
				"required": ["label_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_prompt_label",
			"description": "Update a prompt label's name, description, or color only, unlike update_prompt_version which changes which label a version carries. This takes effect immediately for all versions already tagged with the label, but does not reassign labels or touch history; use list_prompt_labels to find the label_id first.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"label_id": {
						"type": "string",
						"description": "Label ID to update"
					},
					"name": {
						"description": "New name for the label",
						"type": "string"
					},
					"description": {
						"description": "New description",
						"type": "string"
					},
					"color_code": {
						"description": "New hex color code (e.g., '#FF5733')",
						"type": "string",
						"pattern": "^#[0-9A-Fa-f]{6}$"
					}
				},
				"required": ["label_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "delete_prompt_label",
			"description": "Delete a prompt label by ID. This cannot be undone; versions carrying the label lose it, and any workflow resolving by that label will need a replacement.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"label_id": {
						"type": "string",
						"description": "Label ID to delete"
					}
				},
				"required": ["label_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "create_prompt_partial",
			"description": "Create a reusable prompt partial for inclusion with {{> partial_name}}. Use this for shared snippets or macros; returns the partial id, slug, and version id, and the new version stays inactive until published.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"name": {
						"type": "string",
						"description": "Display name for the partial"
					},
					"string": {
						"type": "string",
						"description": "The partial content/template string"
					},
					"workspace_id": {
						"description": "Workspace ID to create partial in (required for org-level API keys)",
						"type": "string"
					},
					"version_description": {
						"description": "Description for this version",
						"type": "string"
					}
				},
				"required": ["name", "string"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_prompt_partials",
			"description": "List partials across collections, with optional collection filtering. Returns ids, slugs, names, collections, and status so you can choose a prompt_partial_id before get_prompt_partial, update_prompt_partial, delete_prompt_partial, or publish_partial.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"collection_id": {
						"description": "Filter by collection ID. Optional — omit to list all partials across collections",
						"type": "string"
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_prompt_partial",
			"description": "Fetch a partial's content and current version details. Use this before embedding, updating, or checking what {{> partial_name}} resolves to; returns the stored string plus version metadata.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"prompt_partial_id": {
						"type": "string",
						"description": "Prompt partial ID or slug to retrieve"
					}
				},
				"required": ["prompt_partial_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_prompt_partial",
			"description": "Create a new version of a partial by updating its content or metadata. Only provided fields change, and the new version stays inactive until publish_partial makes it current.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"prompt_partial_id": {
						"type": "string",
						"description": "Prompt partial ID or slug to update"
					},
					"name": {
						"description": "New display name for the partial",
						"type": "string"
					},
					"string": {
						"description": "New content for the partial",
						"type": "string"
					},
					"description": {
						"description": "Description for this version",
						"type": "string"
					},
					"status": {
						"description": "New status for the partial",
						"type": "string",
						"enum": ["active", "archived"]
					}
				},
				"required": ["prompt_partial_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "delete_prompt_partial",
			"description": "Delete a prompt partial by ID. This cannot be undone, and prompts that reference it with {{> name}} will fail to render until you replace the reference.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"prompt_partial_id": {
						"type": "string",
						"description": "Prompt partial ID or slug to delete"
					}
				},
				"required": ["prompt_partial_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_partial_versions",
			"description": "List all versions for one partial, including version numbers, descriptions, status, and timestamps. Use this when you need history or want to choose a version_id before publish_partial.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"prompt_partial_id": {
						"type": "string",
						"description": "Prompt partial ID or slug to list versions for"
					}
				},
				"required": ["prompt_partial_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "publish_partial",
			"description": "Publish a specific partial version as the default, unlike update_prompt_partial which creates a new draft without activating it. Use after list_partial_versions to pick a version_id; this immediately changes what {{> partial_name}} resolves to for all prompts and replaces the previously active version without a rollback path.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"prompt_partial_id": {
						"type": "string",
						"description": "Prompt partial ID or slug"
					},
					"version": {
						"type": "number",
						"exclusiveMinimum": 0,
						"description": "Version number to publish as default"
					}
				},
				"required": ["prompt_partial_id", "version"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "create_feedback",
			"description": "Create feedback for a trace or request. Writes a new feedback record linked by trace_id, returns the created feedback IDs and status, and takes effect immediately; use update_feedback when correcting an existing record.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"trace_id": {
						"type": "string",
						"description": "The trace ID to associate the feedback with. This links feedback to a specific request/generation."
					},
					"value": {
						"type": "number",
						"description": "Feedback value/rating. Common patterns: 1 for positive (thumbs up), 0 for negative (thumbs down), or use a scale like 1-5."
					},
					"weight": {
						"description": "Optional weighting factor for the feedback. Use to give more importance to certain feedback.",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"metadata": {
						"description": "Optional custom metadata for categorization and analysis (e.g., feedback_source, category, user_segment).",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					}
				},
				"required": ["trace_id", "value"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "update_feedback",
			"description": "Update an existing feedback record by ID. Returns the updated status and feedback IDs, changes only value, weight, and metadata, and leaves the trace linkage immutable; use create_feedback only for a new record.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"description": "The unique identifier of the feedback to update"
					},
					"value": {
						"description": "New feedback value/rating. Common patterns: 1 for positive, 0 for negative.",
						"type": "number"
					},
					"weight": {
						"description": "New weighting factor for the feedback",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"metadata": {
						"description": "New or updated custom metadata for the feedback",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "get_log",
			"description": "Get one gateway request log by ID, including its request, response, usage, cost, and metadata payload when available. For path_format v2, also provide the log's created_at timestamp.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"log_id": {
						"type": "string",
						"description": "Gateway log ID to retrieve"
					},
					"path_format": {
						"description": "Log storage path format; defaults to v1",
						"type": "string",
						"enum": ["v1", "v2"]
					},
					"created_at": {
						"description": "Log creation timestamp in ISO 8601 format; required for v2",
						"type": "string",
						"format": "date-time",
						"pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z|([+-](?:[01]\\d|2[0-3]):[0-5]\\d)))$"
					},
					"type": {
						"description": "Set to hooks when retrieving a hook execution log",
						"type": "string",
						"enum": ["hooks"]
					}
				},
				"required": ["log_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Get Gateway Log",
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_log_export_field_restrictions",
			"description": "Get the organisation-managed fields that a workspace is restricted from including in log exports. Use before create_log_export or update_log_export to avoid requesting disallowed fields. Requires an API key with logs.export scope; completion-log fields may additionally require completion scope. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"workspace_id": {
						"type": "string",
						"description": "Workspace ID whose export field restrictions should be read"
					}
				},
				"required": ["workspace_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Get Log Export Field Restrictions",
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "insert_log",
			"description": "Insert log records for requests that bypassed the gateway. This writes request, response, and trace metadata into Portkey immediately, and the call will fail if request_provider does not match a configured integration. Use the span fields to stitch trace hierarchies together.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"request_url": {
						"description": "The endpoint URL being called",
						"type": "string"
					},
					"request_provider": {
						"description": "AI provider name (e.g., 'openai', 'anthropic')",
						"type": "string"
					},
					"request_method": {
						"default": "post",
						"description": "HTTP method used (defaults to 'post')",
						"type": "string"
					},
					"request_headers": {
						"description": "Request headers as key-value pairs",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {
							"type": "string"
						}
					},
					"request_body": {
						"description": "Request payload/body",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"response_status": {
						"default": 200,
						"description": "HTTP response status code (defaults to 200)",
						"type": "number"
					},
					"response_headers": {
						"description": "Response headers as key-value pairs",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {
							"type": "string"
						}
					},
					"response_body": {
						"description": "Response payload/body",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"response_time": {
						"description": "Response latency in milliseconds",
						"type": "number"
					},
					"streaming_mode": {
						"default": false,
						"description": "Whether the response was streamed",
						"type": "boolean"
					},
					"metadata_organization": {
						"description": "Organization identifier for the log",
						"type": "string"
					},
					"metadata_user": {
						"description": "User identifier for the log",
						"type": "string"
					},
					"metadata_trace_id": {
						"description": "Trace ID for distributed tracing",
						"type": "string"
					},
					"metadata_span_id": {
						"description": "Span ID for tracing",
						"type": "string"
					},
					"metadata_span_name": {
						"description": "Span name for tracing",
						"type": "string"
					},
					"metadata_parent_span_id": {
						"description": "Parent span ID for tracing",
						"type": "string"
					},
					"metadata_custom": {
						"description": "Additional custom metadata key-value pairs",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "create_log_export",
			"description": "Create a log export definition with filters and requested fields. This only sets up the export and does not start processing; call start_log_export next, then use get_log_export or download_log_export to inspect or retrieve the finished result. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"workspace_id": {
						"description": "Workspace ID for the export",
						"type": "string"
					},
					"description": {
						"description": "Human-readable description for the export job",
						"type": "string"
					},
					"time_min": {
						"type": "string",
						"description": "Minimum time filter in date format (e.g., '2024-01-01' or ISO 8601)"
					},
					"time_max": {
						"type": "string",
						"description": "Maximum time filter in date format (e.g., '2024-01-31' or ISO 8601)"
					},
					"cost_min": {
						"description": "Minimum cost filter",
						"type": "number"
					},
					"cost_max": {
						"description": "Maximum cost filter",
						"type": "number"
					},
					"total_units_min": {
						"description": "Minimum total units (tokens) filter",
						"type": "number"
					},
					"total_units_max": {
						"description": "Maximum total units (tokens) filter",
						"type": "number"
					},
					"ai_model": {
						"description": "Filter by specific AI model names",
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"requested_fields": {
						"type": "array",
						"items": {
							"type": "string",
							"enum": [
								"id",
								"trace_id",
								"created_at",
								"request",
								"response",
								"is_success",
								"ai_org",
								"ai_model",
								"req_units",
								"res_units",
								"total_units",
								"request_url",
								"cost",
								"cost_currency",
								"response_time",
								"response_status_code",
								"mode",
								"config",
								"prompt_slug",
								"metadata"
							]
						},
						"description": "Fields to include in export: id, trace_id, created_at, request, response, is_success, ai_org, ai_model, req_units, res_units, total_units, request_url, cost, cost_currency, response_time, response_status_code, mode, config, prompt_slug, metadata"
					}
				},
				"required": ["time_min", "time_max", "requested_fields"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_log_exports",
			"description": "List log export jobs in a workspace with status, filters, and timestamps. Use this to find an export_id before calling get_log_export, start_log_export, cancel_log_export, or download_log_export. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"workspace_id": {
						"type": "string",
						"description": "Workspace ID to list exports for (required)"
					}
				},
				"required": ["workspace_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_log_export",
			"description": "Fetch one log export job by export_id and return its status, filters, requested fields, and file metadata. Use this when you already know the target; use list_log_exports for a workspace-wide overview. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"export_id": {
						"type": "string",
						"description": "The unique ID of the log export"
					}
				},
				"required": ["export_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "start_log_export",
			"description": "Start processing a previously created log export job. This is asynchronous, only queues the export, and does not return rows or a download file; use get_log_export to poll progress and download_log_export after the job completes. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"export_id": {
						"type": "string",
						"description": "The unique ID of the log export to start"
					}
				},
				"required": ["export_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "cancel_log_export",
			"description": "Cancel a pending or running log export job, unlike start_log_export which queues one or delete_integration which removes the source. This permanently stops that export, takes effect immediately, and does not roll back already-processed rows; call create_log_export and start_log_export again to retry. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"export_id": {
						"type": "string",
						"description": "The unique ID of the log export to cancel"
					}
				},
				"required": ["export_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "download_log_export",
			"description": "Get a signed URL for downloading a completed log export. The export must already be finished; use get_log_export to confirm readiness and start_log_export if it has not run yet. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"export_id": {
						"type": "string",
						"description": "The unique ID of the log export to download"
					}
				},
				"required": ["export_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_log_export",
			"description": "Update an existing log export configuration before or between export runs. Only workspace_id, time_of_generation_max, and requested_fields can change after creation, so use get_log_export to review the current job and start_log_export after the definition is ready. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"export_id": {
						"type": "string",
						"description": "The unique ID of the log export to update"
					},
					"workspace_id": {
						"description": "Workspace ID for the export",
						"type": "string"
					},
					"time_of_generation_max": {
						"description": "Maximum time filter in date format (e.g., '2024-07-25' or ISO 8601)",
						"type": "string"
					},
					"requested_fields": {
						"description": "Fields to include in export: id, trace_id, created_at, request, response, is_success, ai_org, ai_model, req_units, res_units, total_units, request_url, cost, cost_currency, response_time, response_status_code, mode, config, prompt_slug, metadata",
						"type": "array",
						"items": {
							"type": "string",
							"enum": [
								"id",
								"trace_id",
								"created_at",
								"request",
								"response",
								"is_success",
								"ai_org",
								"ai_model",
								"req_units",
								"res_units",
								"total_units",
								"request_url",
								"cost",
								"cost_currency",
								"response_time",
								"response_status_code",
								"mode",
								"config",
								"prompt_slug",
								"metadata"
							]
						}
					}
				},
				"required": ["export_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_providers",
			"description": "List workspace-scoped provider instances and their limits or status. Use this to find provider slugs for workspace-level updates; use list_integrations for the org-level source connection. Returns total plus provider name, slug, integration, status, limits, expiration, and reset flags.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Number of results per page (max 100, default 50)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					},
					"workspace_id": {
						"description": "Workspace ID - required when using organization admin keys, optional with workspace API keys",
						"type": "string"
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "create_provider",
			"description": "Create a workspace provider backed by an org integration. The provider inherits the integration key, but its limits and expiration are enforced independently for that workspace. Returns the new provider id and slug.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"name": {
						"type": "string",
						"description": "Display name for the provider"
					},
					"integration_id": {
						"type": "string",
						"description": "Integration slug for the provider (e.g., 'openai', 'anthropic', 'azure-openai')"
					},
					"workspace_id": {
						"description": "Workspace ID - required when using organization admin API keys",
						"type": "string"
					},
					"slug": {
						"description": "Custom slug for the provider. Auto-generated with random suffix if omitted",
						"type": "string"
					},
					"note": {
						"description": "Optional note or description for the provider",
						"type": "string"
					},
					"credit_limit": {
						"description": "Credit limit for usage",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"alert_threshold": {
						"description": "Alert threshold percentage (0-100)",
						"type": "number",
						"minimum": 0,
						"maximum": 100
					},
					"usage_limit_type": {
						"description": "Type of usage limit: 'cost' (monetary) or 'tokens' (token count). Defaults to 'cost'.",
						"type": "string",
						"enum": ["cost", "tokens"]
					},
					"periodic_reset": {
						"description": "Period for resetting usage limits: 'monthly' or 'weekly'. Defaults to 'monthly'.",
						"type": "string",
						"enum": ["monthly", "weekly"]
					},
					"rate_limit_value": {
						"description": "Must be provided together with rate_limit_unit.",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"rate_limit_unit": {
						"description": "Must be provided together with rate_limit_value. Values: 'rpm' (requests/min), 'rph' (requests/hour), or 'rpd' (requests/day).",
						"type": "string",
						"enum": ["rpm", "rph", "rpd"]
					},
					"expires_at": {
						"description": "Expiration date in ISO 8601 format",
						"type": "string"
					}
				},
				"required": ["name", "integration_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "get_provider",
			"description": "Fetch one provider by slug, including limits, rate settings, expiration, and reset status. Use this to check consumption or audit configuration before updating.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"slug": {
						"type": "string",
						"description": "The unique slug identifier of the provider to retrieve"
					},
					"workspace_id": {
						"description": "Workspace ID - required when using organization admin keys",
						"type": "string"
					}
				},
				"required": ["slug"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_provider",
			"description": "Update a provider's metadata, limits, or expiration. reset_usage clears accumulated usage counters immediately, so use it only when you intend to reset quota tracking. Returns the updated provider id and slug.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"slug": {
						"type": "string",
						"description": "The slug of the provider to update"
					},
					"workspace_id": {
						"description": "Workspace ID - required when using organization admin keys",
						"type": "string"
					},
					"name": {
						"description": "New display name for the provider",
						"type": "string"
					},
					"note": {
						"description": "New note or description for the provider",
						"type": "string"
					},
					"credit_limit": {
						"description": "New credit limit for usage",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"alert_threshold": {
						"description": "New alert threshold percentage (0-100)",
						"type": "number",
						"minimum": 0,
						"maximum": 100
					},
					"usage_limit_type": {
						"description": "Type of usage limit: 'cost' (monetary) or 'tokens' (token count). Defaults to 'cost'.",
						"type": "string",
						"enum": ["cost", "tokens"]
					},
					"periodic_reset": {
						"description": "Period for resetting usage limits: 'monthly' or 'weekly'. Defaults to 'monthly'.",
						"type": "string",
						"enum": ["monthly", "weekly"]
					},
					"rate_limit_value": {
						"description": "New rate limit value",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"rate_limit_unit": {
						"description": "Rate limit unit: 'rpm' (requests per minute), 'rph' (requests per hour), or 'rpd' (requests per day)",
						"type": "string",
						"enum": ["rpm", "rph", "rpd"]
					},
					"expires_at": {
						"description": "New expiration date in ISO 8601 format",
						"type": "string"
					},
					"reset_usage": {
						"description": "Set to true to reset accumulated usage metrics",
						"type": "boolean"
					}
				},
				"required": ["slug"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "delete_provider",
			"description": "Delete a workspace provider by slug. This is irreversible and will break prompts, configs, and virtual keys that reference it; use delete_integration for the org source instead. Returns success after the provider is removed.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"slug": {
						"type": "string",
						"description": "The slug of the provider to delete"
					},
					"workspace_id": {
						"description": "Workspace ID - required when using organization admin keys",
						"type": "string"
					}
				},
				"required": ["slug"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "create_secret_reference",
			"description": "Create a reference to a secret stored in AWS Secrets Manager, Azure Key Vault, or HashiCorp Vault. Authentication credentials are sent to Portkey and exposed to this MCP transcript; use short-lived or workload identity modes when possible. Returns the new UUID and slug, never the resolved external secret.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"name": {
						"type": "string",
						"minLength": 1,
						"maxLength": 255,
						"description": "Human-readable name"
					},
					"manager_type": {
						"type": "string",
						"enum": ["aws_sm", "azure_kv", "hashicorp_vault"],
						"description": "External secret manager: aws_sm for AWS Secrets Manager, azure_kv for Azure Key Vault, or hashicorp_vault for HashiCorp Vault; must match the auth_config field family"
					},
					"auth_config": {
						"anyOf": [
							{
								"type": "object",
								"properties": {
									"aws_auth_type": {
										"type": "string",
										"const": "accessKey",
										"description": "Use an AWS access key ID and secret access key"
									},
									"aws_access_key_id": {
										"type": "string",
										"minLength": 1,
										"description": "AWS access key ID; required when aws_auth_type is accessKey"
									},
									"aws_secret_access_key": {
										"type": "string",
										"minLength": 1,
										"description": "AWS secret access key; required when aws_auth_type is accessKey and exposed to the MCP transcript"
									},
									"aws_region": {
										"type": "string",
										"minLength": 1,
										"description": "AWS region containing the secret, for example us-east-1"
									}
								},
								"required": [
									"aws_auth_type",
									"aws_access_key_id",
									"aws_secret_access_key",
									"aws_region"
								],
								"additionalProperties": false
							},
							{
								"type": "object",
								"properties": {
									"aws_auth_type": {
										"type": "string",
										"const": "assumedRole",
										"description": "Assume the specified AWS IAM role"
									},
									"aws_role_arn": {
										"type": "string",
										"minLength": 1,
										"description": "IAM role ARN to assume; required when aws_auth_type is assumedRole"
									},
									"aws_external_id": {
										"anyOf": [
											{
												"type": "string"
											},
											{
												"type": "null"
											}
										],
										"description": "Optional external ID required by the target IAM role"
									},
									"aws_region": {
										"type": "string",
										"minLength": 1,
										"description": "AWS region containing the secret, for example us-east-1"
									}
								},
								"required": ["aws_auth_type", "aws_role_arn", "aws_region"],
								"additionalProperties": false
							},
							{
								"type": "object",
								"properties": {
									"aws_auth_type": {
										"type": "string",
										"const": "serviceRole",
										"description": "Use the AWS workload or service role available at runtime"
									},
									"aws_region": {
										"description": "Optional AWS region containing the secret, for example us-east-1",
										"type": "string"
									}
								},
								"required": ["aws_auth_type"],
								"additionalProperties": false
							},
							{
								"type": "object",
								"properties": {
									"azure_auth_mode": {
										"type": "string",
										"const": "entra",
										"description": "Authenticate with a Microsoft Entra application client secret"
									},
									"azure_entra_tenant_id": {
										"type": "string",
										"minLength": 1,
										"description": "Microsoft Entra tenant ID; required for entra authentication"
									},
									"azure_entra_client_id": {
										"type": "string",
										"minLength": 1,
										"description": "Microsoft Entra application client ID; required for entra authentication"
									},
									"azure_entra_client_secret": {
										"type": "string",
										"minLength": 1,
										"description": "Microsoft Entra client secret; required for entra authentication and exposed to the MCP transcript"
									},
									"azure_vault_url": {
										"type": "string",
										"format": "uri",
										"description": "Azure Key Vault URL, for example https://example.vault.azure.net"
									}
								},
								"required": [
									"azure_auth_mode",
									"azure_entra_tenant_id",
									"azure_entra_client_id",
									"azure_entra_client_secret",
									"azure_vault_url"
								],
								"additionalProperties": false
							},
							{
								"type": "object",
								"properties": {
									"azure_auth_mode": {
										"type": "string",
										"const": "managed",
										"description": "Authenticate with an Azure managed identity"
									},
									"azure_managed_client_id": {
										"type": "string",
										"minLength": 1,
										"description": "Optional client ID of a user-assigned managed identity; omit for the system-assigned identity"
									},
									"azure_vault_url": {
										"type": "string",
										"format": "uri",
										"description": "Azure Key Vault URL, for example https://example.vault.azure.net"
									}
								},
								"required": ["azure_auth_mode", "azure_vault_url"],
								"additionalProperties": false
							},
							{
								"type": "object",
								"properties": {
									"azure_auth_mode": {
										"type": "string",
										"const": "default",
										"description": "Use Azure's default runtime credential chain"
									},
									"azure_vault_url": {
										"type": "string",
										"format": "uri",
										"description": "Azure Key Vault URL, for example https://example.vault.azure.net"
									}
								},
								"required": ["azure_auth_mode", "azure_vault_url"],
								"additionalProperties": false
							},
							{
								"type": "object",
								"properties": {
									"vault_auth_type": {
										"type": "string",
										"const": "token",
										"description": "Authenticate to HashiCorp Vault with a token"
									},
									"vault_addr": {
										"type": "string",
										"format": "uri",
										"description": "Base URL of the HashiCorp Vault server"
									},
									"vault_token": {
										"type": "string",
										"minLength": 1,
										"description": "HashiCorp Vault token; required for token authentication and exposed to the MCP transcript"
									},
									"vault_namespace": {
										"type": "string",
										"minLength": 1,
										"description": "Optional HashiCorp Vault Enterprise namespace"
									}
								},
								"required": ["vault_auth_type", "vault_addr", "vault_token"],
								"additionalProperties": false
							},
							{
								"type": "object",
								"properties": {
									"vault_auth_type": {
										"type": "string",
										"const": "approle",
										"description": "Authenticate to HashiCorp Vault with AppRole credentials"
									},
									"vault_addr": {
										"type": "string",
										"format": "uri",
										"description": "Base URL of the HashiCorp Vault server"
									},
									"vault_role_id": {
										"type": "string",
										"minLength": 1,
										"description": "HashiCorp Vault AppRole role ID; required for approle authentication"
									},
									"vault_secret_id": {
										"type": "string",
										"minLength": 1,
										"description": "HashiCorp Vault AppRole secret ID; required for approle authentication and exposed to the MCP transcript"
									},
									"vault_namespace": {
										"type": "string",
										"minLength": 1,
										"description": "Optional HashiCorp Vault Enterprise namespace"
									}
								},
								"required": [
									"vault_auth_type",
									"vault_addr",
									"vault_role_id",
									"vault_secret_id"
								],
								"additionalProperties": false
							},
							{
								"type": "object",
								"properties": {
									"vault_auth_type": {
										"type": "string",
										"const": "kubernetes",
										"description": "Authenticate to HashiCorp Vault with a Kubernetes service account"
									},
									"vault_addr": {
										"type": "string",
										"format": "uri",
										"description": "Base URL of the HashiCorp Vault server"
									},
									"vault_role": {
										"type": "string",
										"minLength": 1,
										"description": "HashiCorp Vault Kubernetes auth role name"
									},
									"vault_namespace": {
										"type": "string",
										"minLength": 1,
										"description": "Optional HashiCorp Vault Enterprise namespace"
									}
								},
								"required": ["vault_auth_type", "vault_addr", "vault_role"],
								"additionalProperties": false
							}
						],
						"description": "Choose exactly one documented authentication shape matching manager_type. Credentials are sent to Portkey and visible in the MCP transcript; prefer workload identity or short-lived credentials when available."
					},
					"secret_path": {
						"type": "string",
						"maxLength": 1024,
						"description": "Path of the secret in the external secret manager"
					},
					"organisation_id": {
						"description": "Organisation UUID; required when the MCP server is not authenticated with a Portkey API key",
						"type": "string",
						"format": "uuid",
						"pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
					},
					"slug": {
						"description": "Optional stable slug containing only letters, numbers, underscores, or hyphens; Portkey generates one when omitted",
						"type": "string",
						"maxLength": 255,
						"pattern": "^[a-zA-Z0-9_-]+$"
					},
					"description": {
						"description": "Optional description; use null to leave it empty",
						"anyOf": [
							{
								"type": "string",
								"maxLength": 1024
							},
							{
								"type": "null"
							}
						]
					},
					"secret_key": {
						"description": "Optional key within a structured external secret; null uses the whole secret value",
						"anyOf": [
							{
								"type": "string",
								"maxLength": 255
							},
							{
								"type": "null"
							}
						]
					},
					"allow_all_workspaces": {
						"description": "Whether every workspace may use this reference; defaults to true and cannot be true with allowed_workspaces",
						"type": "boolean"
					},
					"allowed_workspaces": {
						"description": "Workspace UUIDs or slugs allowed to use this reference; omit when allow_all_workspaces is true",
						"minItems": 1,
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"tags": {
						"description": "Optional string key-value tags for filtering and organization",
						"anyOf": [
							{
								"type": "object",
								"propertyNames": {
									"type": "string"
								},
								"additionalProperties": {
									"type": "string"
								},
								"description": "Tag filters or metadata as string key-value pairs"
							},
							{
								"type": "null"
							}
						]
					}
				},
				"required": ["name", "manager_type", "auth_config", "secret_path"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Create Secret Reference",
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_secret_references",
			"description": "List Secret References without returning authentication configuration or resolved secret values. Filter by manager type, tags, or name and use a returned UUID or slug with get_secret_reference.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"manager_type": {
						"description": "Filter by external secret manager: aws_sm, azure_kv, or hashicorp_vault",
						"type": "string",
						"enum": ["aws_sm", "azure_kv", "hashicorp_vault"]
					},
					"tags": {
						"description": "Return references matching these tags",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {
							"type": "string"
						}
					},
					"search": {
						"description": "Search references by name",
						"type": "string",
						"minLength": 1,
						"maxLength": 255
					},
					"current_page": {
						"description": "Zero-based page number; defaults to 0",
						"type": "integer",
						"minimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "References per page, from 1 to 100; defaults to 20",
						"type": "integer",
						"minimum": 1,
						"maximum": 100
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "List Secret References",
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_secret_reference",
			"description": "Retrieve one Secret Reference by UUID or slug. Portkey masks sensitive authentication fields for non-system users; the tool returns metadata and the masked auth configuration, not the resolved external secret value.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"minLength": 1,
						"description": "Secret Reference UUID or slug"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Get Secret Reference",
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_secret_reference",
			"description": "Update at least one selected field on a Secret Reference. auth_config is merged into the existing manager-specific configuration. allowed_workspaces replaces all workspace mappings and sets allow_all_workspaces=false; allow_all_workspaces=true removes workspace-specific mappings. Credential values are sent to Portkey and exposed to the MCP transcript.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"minLength": 1,
						"description": "Secret Reference UUID or slug"
					},
					"name": {
						"description": "New human-readable name",
						"type": "string",
						"minLength": 1,
						"maxLength": 255
					},
					"description": {
						"description": "New description, or null to clear it",
						"anyOf": [
							{
								"type": "string",
								"maxLength": 1024
							},
							{
								"type": "null"
							}
						]
					},
					"auth_config": {
						"description": "Provide at least one changed authentication field to merge into the existing configuration; choose one AWS, Azure, or HashiCorp field family and do not mix families",
						"anyOf": [
							{
								"type": "object",
								"properties": {
									"aws_auth_type": {
										"type": "string",
										"enum": ["accessKey", "assumedRole", "serviceRole"],
										"description": "AWS authentication mode: accessKey uses a key pair, assumedRole uses an IAM role, and serviceRole uses the runtime workload identity"
									},
									"aws_access_key_id": {
										"type": "string",
										"minLength": 1,
										"description": "AWS access key ID; required when aws_auth_type is accessKey"
									},
									"aws_secret_access_key": {
										"type": "string",
										"minLength": 1,
										"description": "AWS secret access key; required when aws_auth_type is accessKey and exposed to the MCP transcript"
									},
									"aws_region": {
										"description": "AWS region containing the secret, for example us-east-1",
										"type": "string"
									},
									"aws_role_arn": {
										"type": "string",
										"minLength": 1,
										"description": "IAM role ARN to assume; required when aws_auth_type is assumedRole"
									},
									"aws_external_id": {
										"anyOf": [
											{
												"type": "string"
											},
											{
												"type": "null"
											}
										],
										"description": "Optional external ID required by the target IAM role"
									}
								},
								"additionalProperties": false
							},
							{
								"type": "object",
								"properties": {
									"azure_auth_mode": {
										"type": "string",
										"enum": ["entra", "managed", "default"],
										"description": "Azure authentication mode: entra uses a client secret, managed uses managed identity, and default uses the runtime credential chain"
									},
									"azure_entra_tenant_id": {
										"type": "string",
										"minLength": 1,
										"description": "Microsoft Entra tenant ID; required for entra authentication"
									},
									"azure_entra_client_id": {
										"type": "string",
										"minLength": 1,
										"description": "Microsoft Entra application client ID; required for entra authentication"
									},
									"azure_entra_client_secret": {
										"type": "string",
										"minLength": 1,
										"description": "Microsoft Entra client secret; required for entra authentication and exposed to the MCP transcript"
									},
									"azure_managed_client_id": {
										"description": "Optional client ID of a user-assigned managed identity; omit for the system-assigned identity",
										"type": "string"
									},
									"azure_vault_url": {
										"type": "string",
										"format": "uri",
										"description": "Azure Key Vault URL, for example https://example.vault.azure.net"
									}
								},
								"additionalProperties": false
							},
							{
								"type": "object",
								"properties": {
									"vault_auth_type": {
										"type": "string",
										"enum": ["token", "approle", "kubernetes"],
										"description": "HashiCorp Vault authentication mode: token, AppRole, or Kubernetes"
									},
									"vault_addr": {
										"type": "string",
										"format": "uri",
										"description": "Base URL of the HashiCorp Vault server"
									},
									"vault_token": {
										"type": "string",
										"minLength": 1,
										"description": "HashiCorp Vault token; required for token authentication and exposed to the MCP transcript"
									},
									"vault_namespace": {
										"description": "Optional HashiCorp Vault Enterprise namespace",
										"type": "string"
									},
									"vault_role_id": {
										"type": "string",
										"minLength": 1,
										"description": "HashiCorp Vault AppRole role ID; required for approle authentication"
									},
									"vault_secret_id": {
										"type": "string",
										"minLength": 1,
										"description": "HashiCorp Vault AppRole secret ID; required for approle authentication and exposed to the MCP transcript"
									},
									"vault_role": {
										"type": "string",
										"minLength": 1,
										"description": "HashiCorp Vault Kubernetes auth role name"
									}
								},
								"additionalProperties": false
							}
						]
					},
					"secret_path": {
						"description": "New path of the secret in the external secret manager",
						"type": "string",
						"maxLength": 1024
					},
					"secret_key": {
						"description": "New key within a structured secret, or null to use the whole value",
						"anyOf": [
							{
								"type": "string",
								"maxLength": 255
							},
							{
								"type": "null"
							}
						]
					},
					"allow_all_workspaces": {
						"description": "Whether every workspace may use this reference; cannot be true with allowed_workspaces",
						"type": "boolean"
					},
					"allowed_workspaces": {
						"description": "Replacement list of workspace UUIDs or slugs allowed to use this reference; supplying it sets allow_all_workspaces=false",
						"minItems": 1,
						"type": "array",
						"items": {
							"type": "string"
						}
					},
					"tags": {
						"description": "Replacement string key-value tags, or null to clear them",
						"anyOf": [
							{
								"type": "object",
								"propertyNames": {
									"type": "string"
								},
								"additionalProperties": {
									"type": "string"
								},
								"description": "Tag filters or metadata as string key-value pairs"
							},
							{
								"type": "null"
							}
						]
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Update Secret Reference",
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "delete_secret_reference",
			"description": "Permanently delete a Secret Reference. This is irreversible and integrations or virtual keys using it can fail to resolve credentials immediately; confirm dependencies before deletion.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"minLength": 1,
						"description": "Secret Reference UUID or slug"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Delete Secret Reference",
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "get_model_pricing",
			"description": "Get Portkey's current public pricing configuration for one exact provider/model pair. Prices are returned in USD cents per token or provider-specific unit and may include cache, audio, image, fine-tuning, and calculation metadata. Use this read-only catalog lookup before setting integration pricing_adjustments or custom-model pricing; it does not return your negotiated integration multiplier or require Portkey authentication.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"provider": {
						"type": "string",
						"description": "Lowercase Portkey provider identifier, such as openai, anthropic, bedrock, or x-ai"
					},
					"model": {
						"type": "string",
						"description": "Exact provider model identifier, such as gpt-4o"
					}
				},
				"required": ["provider", "model"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Get Model Pricing",
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "list_integrations",
			"description": "List org-level AI provider connections with optional workspace or type filters. Use this to find integration slugs before model or workspace updates. Returns total plus id, name, slug, provider, status, description, workspace counts, and config summary.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Number of results per page (default 100, max 100)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					},
					"workspace_id": {
						"description": "Filter integrations accessible by a specific workspace",
						"type": "string"
					},
					"type": {
						"description": "Filter by integration type: 'workspace', 'organisation', or 'all' (default)",
						"type": "string",
						"enum": ["workspace", "organisation", "all"]
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "create_integration",
			"description": "Create an AI-provider integration that becomes the source for workspace providers. ai_provider_id identifies the backend; provider-specific fields configure Azure, Bedrock, Vertex, or custom hosts. For workspace-scoped integrations, create_default_provider controls automatic provider creation. key is write-only, but secret_mappings can resolve it or configuration fields from Secret References at runtime. pricing_adjustments apply negotiated discounts or markups to cost accounting. Use update_integration_models and update_integration_workspaces after creation; returns the new integration id and slug.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"name": {
						"type": "string",
						"description": "Human-readable name for the integration"
					},
					"ai_provider_id": {
						"type": "string",
						"description": "ID of the AI provider (e.g., 'openai', 'anthropic', 'azure-openai', 'aws-bedrock', 'vertex-ai')"
					},
					"slug": {
						"description": "URL-friendly identifier (auto-generated from name if not provided)",
						"type": "string"
					},
					"key": {
						"description": "API key for the provider (if required)",
						"type": "string"
					},
					"description": {
						"description": "Optional description of the integration",
						"type": "string"
					},
					"workspace_id": {
						"description": "Workspace ID for workspace-scoped integrations",
						"type": "string"
					},
					"api_version": {
						"description": "API version (for Azure OpenAI)",
						"type": "string"
					},
					"resource_name": {
						"description": "Resource name (for Azure OpenAI)",
						"type": "string"
					},
					"deployment_name": {
						"description": "Deployment name (for Azure OpenAI)",
						"type": "string"
					},
					"aws_region": {
						"description": "AWS region (for AWS Bedrock)",
						"type": "string"
					},
					"aws_access_key_id": {
						"description": "AWS access key ID (for AWS Bedrock)",
						"type": "string"
					},
					"aws_secret_access_key": {
						"description": "AWS secret access key (for AWS Bedrock)",
						"type": "string"
					},
					"vertex_project_id": {
						"description": "GCP project ID (for Vertex AI)",
						"type": "string"
					},
					"vertex_region": {
						"description": "GCP region (for Vertex AI)",
						"type": "string"
					},
					"custom_host": {
						"description": "Custom base URL for the provider",
						"type": "string"
					},
					"create_default_provider": {
						"description": "Create a provider automatically for a workspace-scoped integration; defaults to true",
						"type": "boolean"
					},
					"default_provider_slug": {
						"description": "Custom slug for the automatically created workspace provider",
						"type": "string",
						"maxLength": 255,
						"pattern": "^[a-zA-Z0-9_-]+$"
					},
					"secret_mappings": {
						"description": "Unique runtime Secret Reference mappings; mapping key allows the key field to be omitted",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"target_field": {
									"type": "string",
									"description": "Integration field populated at runtime: key or configurations.<field>"
								},
								"secret_reference_id": {
									"type": "string",
									"description": "Secret Reference UUID or slug accessible to the workspace"
								},
								"secret_key": {
									"description": "Optional key to select from a multi-value secret",
									"anyOf": [
										{
											"type": "string"
										},
										{
											"type": "null"
										}
									]
								},
								"value_format": {
									"description": "Treat the resolved value as a plain string or parsed JSON",
									"anyOf": [
										{
											"type": "string",
											"enum": ["string", "json"]
										},
										{
											"type": "null"
										}
									]
								}
							},
							"required": ["target_field", "secret_reference_id"]
						}
					},
					"pricing_adjustments": {
						"description": "Negotiated discount or markup multipliers for cost accounting",
						"anyOf": [
							{
								"type": "object",
								"properties": {
									"multiplier": {
										"description": "Per-unit multipliers applied over Portkey catalog pricing",
										"type": "object",
										"properties": {
											"default": {
												"description": "Fallback multiplier for pricing units; 1 is unchanged, 0.8 is a 20% discount",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"request_token": {
												"description": "Input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"response_token": {
												"description": "Output token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"cache_read_input_token": {
												"description": "Cache-read input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"cache_write_input_token": {
												"description": "Cache-write input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"cache_read_audio_input_token": {
												"description": "Cache-read audio input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"request_audio_token": {
												"description": "Audio input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"response_audio_token": {
												"description": "Audio output token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"reasoning_token": {
												"description": "Reasoning token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"prediction_accepted_token": {
												"description": "Accepted predicted-output token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"prediction_rejected_token": {
												"description": "Rejected predicted-output token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"request_image_token": {
												"description": "Image input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"response_image_token": {
												"description": "Image output token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"request_text_token": {
												"description": "Text input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"response_text_token": {
												"description": "Text output token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"cache_read_image_input_token": {
												"description": "Cache-read image input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"cache_read_text_input_token": {
												"description": "Cache-read text input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"cache_write_text_input_token": {
												"description": "Cache-write text input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"cache_write_image_input_token": {
												"description": "Cache-write image input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"image": {
												"description": "Image-generation pricing multipliers",
												"anyOf": [
													{
														"type": "object",
														"properties": {
															"default": {
																"description": "Default image-generation price multiplier",
																"anyOf": [
																	{
																		"type": "number",
																		"minimum": 0
																	},
																	{
																		"type": "null"
																	}
																]
															}
														}
													},
													{
														"type": "null"
													}
												]
											},
											"additional_units": {
												"description": "Multipliers keyed by Portkey pricing unit, such as web_search or file_search",
												"anyOf": [
													{
														"type": "object",
														"propertyNames": {
															"type": "string"
														},
														"additionalProperties": {
															"anyOf": [
																{
																	"type": "number",
																	"minimum": 0
																},
																{
																	"type": "null"
																}
															]
														}
													},
													{
														"type": "null"
													}
												]
											}
										}
									}
								}
							},
							{
								"type": "null"
							}
						]
					}
				},
				"required": ["name", "ai_provider_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Create AI Provider Integration",
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "get_integration",
			"description": "Fetch one integration by slug, including masked key, workspace access, allowed models, and configuration metadata. Use this before editing provider-specific settings or auditing access. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"slug": {
						"type": "string",
						"description": "The unique slug identifier of the integration to retrieve"
					}
				},
				"required": ["slug"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_integration",
			"description": "Update an integration's name, description, API key, provider config, Secret Reference mappings, or pricing adjustments by slug. Only provided fields change; key, secret mapping, and config changes take effect immediately and can disrupt dependent providers or live requests, while pricing multipliers change cost analytics and budget accounting. Review get_integration first. Model availability and workspace access remain separate in update_integration_models and update_integration_workspaces.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"slug": {
						"type": "string",
						"description": "The slug of the integration to update"
					},
					"name": {
						"description": "New human-readable name for the integration",
						"type": "string"
					},
					"key": {
						"description": "New API key for the provider",
						"type": "string"
					},
					"description": {
						"description": "New description for the integration",
						"type": "string"
					},
					"api_version": {
						"description": "New API version (for Azure OpenAI)",
						"type": "string"
					},
					"resource_name": {
						"description": "New resource name (for Azure OpenAI)",
						"type": "string"
					},
					"deployment_name": {
						"description": "New deployment name (for Azure OpenAI)",
						"type": "string"
					},
					"aws_region": {
						"description": "New AWS region (for AWS Bedrock)",
						"type": "string"
					},
					"aws_access_key_id": {
						"description": "New AWS access key ID (for AWS Bedrock)",
						"type": "string"
					},
					"aws_secret_access_key": {
						"description": "New AWS secret access key (for AWS Bedrock)",
						"type": "string"
					},
					"vertex_project_id": {
						"description": "New GCP project ID (for Vertex AI)",
						"type": "string"
					},
					"vertex_region": {
						"description": "New GCP region (for Vertex AI)",
						"type": "string"
					},
					"custom_host": {
						"description": "New custom base URL for the provider",
						"type": "string"
					},
					"secret_mappings": {
						"description": "Replacement runtime Secret Reference mappings; each target_field must be unique",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"target_field": {
									"type": "string",
									"description": "Integration field populated at runtime: key or configurations.<field>"
								},
								"secret_reference_id": {
									"type": "string",
									"description": "Secret Reference UUID or slug accessible to the workspace"
								},
								"secret_key": {
									"description": "Optional key to select from a multi-value secret",
									"anyOf": [
										{
											"type": "string"
										},
										{
											"type": "null"
										}
									]
								},
								"value_format": {
									"description": "Treat the resolved value as a plain string or parsed JSON",
									"anyOf": [
										{
											"type": "string",
											"enum": ["string", "json"]
										},
										{
											"type": "null"
										}
									]
								}
							},
							"required": ["target_field", "secret_reference_id"]
						}
					},
					"pricing_adjustments": {
						"description": "Replacement cost multiplier configuration, or null to clear adjustments",
						"anyOf": [
							{
								"type": "object",
								"properties": {
									"multiplier": {
										"description": "Per-unit multipliers applied over Portkey catalog pricing",
										"type": "object",
										"properties": {
											"default": {
												"description": "Fallback multiplier for pricing units; 1 is unchanged, 0.8 is a 20% discount",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"request_token": {
												"description": "Input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"response_token": {
												"description": "Output token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"cache_read_input_token": {
												"description": "Cache-read input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"cache_write_input_token": {
												"description": "Cache-write input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"cache_read_audio_input_token": {
												"description": "Cache-read audio input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"request_audio_token": {
												"description": "Audio input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"response_audio_token": {
												"description": "Audio output token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"reasoning_token": {
												"description": "Reasoning token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"prediction_accepted_token": {
												"description": "Accepted predicted-output token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"prediction_rejected_token": {
												"description": "Rejected predicted-output token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"request_image_token": {
												"description": "Image input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"response_image_token": {
												"description": "Image output token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"request_text_token": {
												"description": "Text input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"response_text_token": {
												"description": "Text output token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"cache_read_image_input_token": {
												"description": "Cache-read image input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"cache_read_text_input_token": {
												"description": "Cache-read text input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"cache_write_text_input_token": {
												"description": "Cache-write text input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"cache_write_image_input_token": {
												"description": "Cache-write image input token price multiplier",
												"anyOf": [
													{
														"type": "number",
														"minimum": 0
													},
													{
														"type": "null"
													}
												]
											},
											"image": {
												"description": "Image-generation pricing multipliers",
												"anyOf": [
													{
														"type": "object",
														"properties": {
															"default": {
																"description": "Default image-generation price multiplier",
																"anyOf": [
																	{
																		"type": "number",
																		"minimum": 0
																	},
																	{
																		"type": "null"
																	}
																]
															}
														}
													},
													{
														"type": "null"
													}
												]
											},
											"additional_units": {
												"description": "Multipliers keyed by Portkey pricing unit, such as web_search or file_search",
												"anyOf": [
													{
														"type": "object",
														"propertyNames": {
															"type": "string"
														},
														"additionalProperties": {
															"anyOf": [
																{
																	"type": "number",
																	"minimum": 0
																},
																{
																	"type": "null"
																}
															]
														}
													},
													{
														"type": "null"
													}
												]
											}
										}
									}
								}
							},
							{
								"type": "null"
							}
						]
					}
				},
				"required": ["slug"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Update AI Provider Integration",
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "delete_integration",
			"description": "Delete an integration by slug. This is irreversible and stops the org-level connection, which will break dependent virtual keys, providers, and workspace access.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"slug": {
						"type": "string",
						"description": "The slug of the integration to delete"
					}
				},
				"required": ["slug"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_integration_models",
			"description": "List models enabled on an integration. Use this to verify model availability before creating prompts or configs. Returns total plus model ids, display names, enabled state, and custom-model markers. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"slug": {
						"type": "string",
						"description": "The slug of the integration"
					},
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Number of results per page",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					}
				},
				"required": ["slug"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_integration_models",
			"description": "Bulk enable or disable integration models, register custom or fine-tuned models, set per-model hosts and headers, and attach static token pricing. allow_all_models controls whether future provider models start enabled. These changes affect every workspace using the integration; inspect list_integration_models first and use get_model_pricing when deriving custom rates. Returns success and the number of models updated.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"slug": {
						"type": "string",
						"description": "The slug of the integration"
					},
					"allow_all_models": {
						"description": "Whether newly available provider models are enabled by default",
						"type": "boolean"
					},
					"models": {
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"slug": {
									"type": "string",
									"description": "The model slug identifier"
								},
								"model_name": {
									"description": "Display name for the model (required for custom models)",
									"type": "string"
								},
								"enabled": {
									"type": "boolean",
									"description": "Whether the model is enabled"
								},
								"is_custom": {
									"description": "Whether this is a custom model (default: false)",
									"type": "boolean"
								},
								"is_finetune": {
									"description": "Whether this entry is a fine-tuned model",
									"type": "boolean"
								},
								"base_model_slug": {
									"description": "Base model slug for a fine-tuned model",
									"type": "string"
								},
								"configurations": {
									"description": "Per-model custom upstream host and headers",
									"type": "object",
									"properties": {
										"custom_host": {
											"description": "Custom upstream URL used only for this model",
											"type": "string",
											"format": "uri"
										},
										"custom_headers": {
											"description": "Headers sent only for this model; values may contain secrets",
											"type": "object",
											"propertyNames": {
												"type": "string"
											},
											"additionalProperties": {
												"type": "string"
											}
										}
									}
								},
								"pricing_config": {
									"description": "Static custom pricing for this integration model",
									"type": "object",
									"properties": {
										"type": {
											"type": "string",
											"const": "static",
											"description": "Static per-token pricing configuration"
										},
										"pay_as_you_go": {
											"description": "Pay-as-you-go rates for this integration model",
											"type": "object",
											"properties": {
												"request_token": {
													"description": "Input token pricing",
													"type": "object",
													"properties": {
														"price": {
															"type": "number",
															"minimum": 0,
															"description": "Static price per token in the integration pricing unit"
														}
													},
													"required": ["price"]
												},
												"response_token": {
													"description": "Output token pricing",
													"type": "object",
													"properties": {
														"price": {
															"type": "number",
															"minimum": 0,
															"description": "Static price per token in the integration pricing unit"
														}
													},
													"required": ["price"]
												}
											}
										}
									},
									"required": ["type"]
								}
							},
							"required": ["slug", "enabled"]
						},
						"description": "Array of model configurations to update"
					}
				},
				"required": ["slug", "models"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Update Integration Models",
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "delete_integration_model",
			"description": "Delete a custom model from an integration. Built-in models should be disabled instead, because deletion only applies to custom entries. Returns success after the custom model is removed.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"slug": {
						"type": "string",
						"description": "The slug of the integration"
					},
					"model_slug": {
						"type": "string",
						"description": "The slug of the model to delete"
					}
				},
				"required": ["slug", "model_slug"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_integration_workspaces",
			"description": "List workspaces that can use an integration, with their limits. Use this to audit access or confirm per-workspace cost and rate settings. Returns total plus workspace ids, names, enabled state, usage limits, and rate limits. Enterprise-gated. Returns 403 on non-Enterprise Portkey plans.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"slug": {
						"type": "string",
						"description": "The slug of the integration"
					},
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Number of results per page",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					}
				},
				"required": ["slug"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_integration_workspaces",
			"description": "Control per-workspace and global access to an integration, including cost/rate limits, usage resets, and automatic default-provider creation. global_workspace_access_enabled affects current and future workspaces; override_existing_workspace_access determines whether it replaces explicit workspace settings. Per-workspace default-provider fields override top-level values. Review list_integration_workspaces first because access and limits change downstream usage immediately.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"slug": {
						"type": "string",
						"description": "The slug of the integration"
					},
					"workspaces": {
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"id": {
									"type": "string",
									"description": "The workspace ID"
								},
								"enabled": {
									"type": "boolean",
									"description": "Whether the workspace has access to this integration"
								},
								"credit_limit": {
									"description": "Credit limit for this workspace",
									"type": "number",
									"exclusiveMinimum": 0
								},
								"alert_threshold": {
									"description": "Alert threshold percentage (0-100)",
									"type": "number",
									"minimum": 0,
									"maximum": 100
								},
								"rate_limit_rpm": {
									"description": "Rate limit in requests per minute",
									"type": "number",
									"exclusiveMinimum": 0
								},
								"reset_usage": {
									"description": "Reset exhausted usage status to active",
									"type": "boolean"
								},
								"create_default_provider": {
									"description": "Override whether a provider is auto-created for this workspace",
									"type": "boolean"
								},
								"default_provider_slug": {
									"description": "Override the auto-created provider slug for this workspace",
									"type": "string"
								}
							},
							"required": ["id", "enabled"]
						},
						"description": "Array of workspace configurations to update"
					},
					"global_workspace_access_enabled": {
						"description": "Enable or disable access for all current and future workspaces",
						"type": "boolean"
					},
					"global_credit_limit": {
						"description": "Global cost credit limit applied with global workspace access",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"global_alert_threshold": {
						"description": "Global cost alert threshold applied to workspace access",
						"type": "number",
						"minimum": 0,
						"maximum": 100
					},
					"global_rate_limit_rpm": {
						"description": "Global requests-per-minute limit for workspace access",
						"type": "number",
						"exclusiveMinimum": 0
					},
					"override_existing_workspace_access": {
						"description": "Apply global settings over existing per-workspace access",
						"type": "boolean"
					},
					"create_default_provider": {
						"description": "Auto-create providers when granting workspace access",
						"type": "boolean"
					},
					"default_provider_slug": {
						"description": "Default slug for providers auto-created in workspaces",
						"type": "string"
					}
				},
				"required": ["slug", "workspaces"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Update Integration Workspace Access",
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "list_mcp_integrations",
			"description": "List MCP integrations in the organization. Returns paginated integration records plus total and has_more for discovering integration IDs; use get_mcp_integration for one integration's full Portkey-side config and list_mcp_servers for the servers under an integration.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"minimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Number of results per page (max 1000)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 1000
					},
					"workspace_id": {
						"description": "Filter by workspace ID",
						"type": "string"
					},
					"organisation_id": {
						"description": "Filter by organisation UUID",
						"type": "string"
					},
					"type": {
						"description": "Filter by workspace, organisation, or all integration ownership",
						"type": "string",
						"enum": ["workspace", "organisation", "all"]
					},
					"search": {
						"description": "Search integrations by name",
						"type": "string"
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "create_mcp_integration",
			"description": "Create a Portkey integration for an external MCP server URL. For headers auth, provide custom_headers or a Secret Reference mapping targeting configurations.custom_headers; secret_mappings resolve protected values at runtime without storing them in the tool call. Organisation admin keys normally need workspace_id. After creation, create_mcp_server and configure capabilities/access; returns the integration id and slug.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"name": {
						"type": "string",
						"description": "Display name for the MCP integration"
					},
					"url": {
						"type": "string",
						"description": "URL endpoint of the MCP server to integrate"
					},
					"auth_type": {
						"type": "string",
						"minLength": 1,
						"description": "Authentication type. Current examples are 'none', 'headers' (custom headers), and 'oauth_auto' (OAuth); Portkey may accept additional values."
					},
					"transport": {
						"type": "string",
						"enum": ["http", "sse"],
						"description": "MCP transport protocol: 'http' (streamable HTTP) or 'sse' (server-sent events)"
					},
					"slug": {
						"description": "Custom slug. Auto-generated if omitted",
						"type": "string"
					},
					"description": {
						"description": "Description of the MCP integration",
						"type": "string"
					},
					"custom_headers": {
						"description": "Custom headers for authentication (e.g. { \"Authorization\": \"Bearer xxx\" }). Sent via configurations.custom_headers",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {
							"type": "string"
						}
					},
					"configurations": {
						"description": "Additional documented or forward-compatible configuration fields. For headers auth, configurations.custom_headers is a string-to-string header map.",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"workspace_id": {
						"description": "Workspace ID — required when using organization admin API keys",
						"type": "string"
					},
					"secret_mappings": {
						"description": "Runtime Secret Reference mappings; every configurations.<field> target must be unique",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"target_field": {
									"type": "string",
									"description": "Configuration field resolved at runtime, such as configurations.oauth_metadata"
								},
								"secret_reference_id": {
									"type": "string",
									"description": "Secret Reference UUID or slug accessible to this integration"
								},
								"secret_key": {
									"description": "Optional key to select from a multi-value secret",
									"anyOf": [
										{
											"type": "string"
										},
										{
											"type": "null"
										}
									]
								},
								"value_format": {
									"description": "Use json when the target configuration field expects a structured object",
									"anyOf": [
										{
											"type": "string",
											"enum": ["string", "json"]
										},
										{
											"type": "null"
										}
									]
								}
							},
							"required": ["target_field", "secret_reference_id"]
						}
					}
				},
				"required": ["name", "url", "auth_type", "transport"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Create MCP Integration",
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "get_mcp_integration",
			"description": "Retrieve one MCP integration by id or slug. Returns the full Portkey-side config, including auth type, transport, and masked configuration keys; use get_mcp_integration_metadata for the server's self-reported metadata.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"description": "The MCP integration ID or slug to retrieve"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_mcp_integration",
			"description": "Update an MCP integration's name, description, URL, auth, transport, headers, or runtime Secret Reference mappings. Only supplied fields change; URL, auth, header, and secret changes apply immediately and can break active clients, so inspect get_mcp_integration first. Use update_mcp_server when changing only a Portkey server instance's display metadata.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"description": "The MCP integration ID or slug to update"
					},
					"name": {
						"description": "New display name",
						"type": "string"
					},
					"description": {
						"description": "New description",
						"type": "string"
					},
					"url": {
						"description": "New URL endpoint",
						"type": "string"
					},
					"auth_type": {
						"description": "New authentication type. Current examples are 'none', 'headers' (custom headers), and 'oauth_auto' (OAuth); Portkey may accept additional values.",
						"type": "string",
						"minLength": 1
					},
					"transport": {
						"description": "New transport protocol",
						"type": "string",
						"enum": ["http", "sse"]
					},
					"custom_headers": {
						"description": "New custom headers for authentication. Sent via configurations.custom_headers",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {
							"type": "string"
						}
					},
					"configurations": {
						"description": "Replacement documented or forward-compatible configuration fields",
						"type": "object",
						"propertyNames": {
							"type": "string"
						},
						"additionalProperties": {}
					},
					"secret_mappings": {
						"description": "Replacement runtime Secret Reference mappings; each target_field must be unique",
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"target_field": {
									"type": "string",
									"description": "Configuration field resolved at runtime, such as configurations.oauth_metadata"
								},
								"secret_reference_id": {
									"type": "string",
									"description": "Secret Reference UUID or slug accessible to this integration"
								},
								"secret_key": {
									"description": "Optional key to select from a multi-value secret",
									"anyOf": [
										{
											"type": "string"
										},
										{
											"type": "null"
										}
									]
								},
								"value_format": {
									"description": "Use json when the target configuration field expects a structured object",
									"anyOf": [
										{
											"type": "string",
											"enum": ["string", "json"]
										},
										{
											"type": "null"
										}
									]
								}
							},
							"required": ["target_field", "secret_reference_id"]
						}
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Update MCP Integration",
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "delete_mcp_integration",
			"description": "Delete an MCP integration and all servers beneath it. This is irreversible, removes connected access immediately, and should only be used after confirming nothing depends on the integration.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"description": "The MCP integration ID or slug to delete"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "get_mcp_integration_metadata",
			"description": "Retrieve the external MCP server's self-reported metadata for an integration. Returns name, version, protocol, capability flags, and sync status; use get_mcp_integration for the Portkey-side connection config.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"description": "The MCP integration ID or slug"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "list_mcp_integration_capabilities",
			"description": "List capabilities exposed by the external MCP server for an integration. Returns total plus enabled-state entries so you can decide what to toggle; use before update_mcp_integration_capabilities when you need to compare the current surface.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"description": "The MCP integration ID or slug"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_mcp_integration_capabilities",
			"description": "Bulk enable or disable capabilities (tools, prompts, resources) on an MCP integration. A reversible toggle, not a deletion: only the capabilities named in the array change state, the change hides or exposes them immediately for connected users, and re-running with enabled flipped restores them. Source the integration id from list_mcp_integrations and current capability names, types, and states from list_mcp_integration_capabilities. Returns a success confirmation message.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"description": "The MCP integration ID or slug"
					},
					"capabilities": {
						"minItems": 1,
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"name": {
									"type": "string",
									"description": "Capability name"
								},
								"type": {
									"type": "string",
									"enum": ["tool", "prompt", "resource"],
									"description": "Capability type"
								},
								"enabled": {
									"type": "boolean",
									"description": "Whether to enable the capability"
								}
							},
							"required": ["name", "type", "enabled"]
						},
						"description": "Array of capability updates"
					}
				},
				"required": ["id", "capabilities"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": false
			}
		},
		{
			"name": "list_mcp_integration_workspaces",
			"description": "List which workspaces can access an MCP integration. Returns the global access mode plus per-workspace enablement for audit or permission review; use before update_mcp_integration_workspaces.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"description": "The MCP integration ID or slug"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_mcp_integration_workspaces",
			"description": "Grant or revoke workspace access to an MCP integration in bulk. Reversible: only the workspaces listed change, access applies or is removed immediately for all users in those workspaces, and re-running with enabled flipped undoes a change. Source the integration id from list_mcp_integrations, workspace ids from list_workspaces, and the current access state from list_mcp_integration_workspaces. Returns a success confirmation message.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"description": "The MCP integration ID or slug"
					},
					"workspaces": {
						"minItems": 1,
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"id": {
									"type": "string",
									"description": "Workspace ID"
								},
								"enabled": {
									"type": "boolean",
									"description": "Whether workspace has access"
								}
							},
							"required": ["id", "enabled"]
						},
						"description": "Array of workspace access updates"
					}
				},
				"required": ["id", "workspaces"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": false
			}
		},
		{
			"name": "list_mcp_server_connections",
			"description": "List active connection records for one Portkey-managed MCP server, including user, connected state, and connection timestamps. Use it to audit sessions or identify the user/workspace before disconnect_mcp_server_connection; service keys can list all users, while user keys default to their own user. Organisation admin keys must provide workspace_id. This reads live connection state and does not test upstream server reachability (use test_mcp_server for that).",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"description": "MCP server ID or slug whose connections to list"
					},
					"user_id": {
						"description": "User UUID to filter; user-scoped keys default to their own user when omitted",
						"type": "string",
						"format": "uuid",
						"pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
					},
					"workspace_id": {
						"description": "Workspace ID or slug; required with an organisation admin API key",
						"type": "string"
					},
					"current_page": {
						"description": "Zero-based results page; defaults to 0",
						"type": "integer",
						"minimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Connections per page, from 1 through 500; defaults to 100",
						"type": "integer",
						"minimum": 1,
						"maximum": 500
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "List MCP Server Connections",
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "disconnect_mcp_server_connection",
			"description": "Disconnect one user's active session from a Portkey-managed MCP server. This immediately ends that connection but does not revoke future access; use update_mcp_server_user_access when access itself should be removed. Provide user_id with service keys (user keys derive it), and workspace_id with organisation admin keys. Inspect list_mcp_server_connections first when the target is uncertain.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"description": "MCP server ID or slug containing the active connection"
					},
					"user_id": {
						"description": "User UUID to disconnect; required with service keys and derived from user keys when omitted",
						"type": "string",
						"format": "uuid",
						"pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
					},
					"workspace_id": {
						"description": "Workspace ID or slug; required with an organisation admin API key",
						"type": "string"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"title": "Disconnect MCP Server Connection",
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "list_mcp_servers",
			"description": "List MCP servers in the organization. Returns paginated server records plus total for discovering server IDs; use get_mcp_server for one server's details and list_mcp_integrations for the parent integration.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Number of results per page (max 100)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					},
					"workspace_id": {
						"description": "Filter by workspace ID",
						"type": "string"
					}
				},
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "create_mcp_server",
			"description": "Create an MCP server under an existing integration. Registers the server and returns the new id and slug; use list_mcp_integrations first to find the parent integration, then capabilities or access tools to configure it.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"name": {
						"type": "string",
						"description": "Display name for the MCP server"
					},
					"mcp_integration_id": {
						"type": "string",
						"description": "ID or slug of the MCP integration this server belongs to"
					},
					"slug": {
						"description": "Custom slug. Auto-generated if omitted",
						"type": "string"
					},
					"description": {
						"description": "Description of the MCP server",
						"type": "string"
					}
				},
				"required": ["name", "mcp_integration_id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "get_mcp_server",
			"description": "Retrieve one MCP server by id or slug. Returns server details including the parent integration, status, and created time; use get_mcp_server when you need the server record rather than the integration config.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"description": "The MCP server ID or slug to retrieve"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_mcp_server",
			"description": "Update an MCP server's name or description. Changes apply immediately, but URL and auth live on the parent integration, so use update_mcp_integration for those fields.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"description": "The MCP server ID or slug to update"
					},
					"name": {
						"description": "New display name",
						"type": "string"
					},
					"description": {
						"description": "New description",
						"type": "string"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "delete_mcp_server",
			"description": "Delete an MCP server instance. This is irreversible, removes connected users' access immediately, and should be used only after confirming no workflows depend on the server.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"description": "The MCP server ID or slug to delete"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "test_mcp_server",
			"description": "Test connectivity to an MCP server. Sends a live check and returns success, response time, HTTP status, and any error; use this before changing configuration or when diagnosing reachability.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"description": "The MCP server ID or slug to test"
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_mcp_server_capabilities",
			"description": "List capabilities exposed by an MCP server instance. Returns total plus the current tool, resource, and prompt surface; use this instead of the integration-level capability list when you need server-specific exposure.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"description": "The MCP server ID or slug"
					},
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Number of results per page (max 100)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_mcp_server_capabilities",
			"description": "Enable or disable capabilities on an MCP server. Changes take effect immediately and override the integration-level settings for this server; use list_mcp_server_capabilities first to inspect the current surface.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"description": "The MCP server ID or slug"
					},
					"capabilities": {
						"minItems": 1,
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"name": {
									"type": "string",
									"description": "Capability name"
								},
								"type": {
									"type": "string",
									"enum": ["tool", "prompt", "resource"],
									"description": "Capability type"
								},
								"enabled": {
									"type": "boolean",
									"description": "Whether to enable the capability"
								}
							},
							"required": ["name", "type", "enabled"]
						},
						"description": "Array of capability updates"
					}
				},
				"required": ["id", "capabilities"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		},
		{
			"name": "list_mcp_server_user_access",
			"description": "List per-user access for an MCP server. Returns the default access mode, override flags, and connection status so you can audit who can use it; use before update_mcp_server_user_access.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"description": "The MCP server ID or slug"
					},
					"current_page": {
						"description": "Page number for pagination",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 9007199254740991
					},
					"page_size": {
						"description": "Number of results per page (max 100)",
						"type": "integer",
						"exclusiveMinimum": 0,
						"maximum": 100
					}
				},
				"required": ["id"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": true,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": true
			}
		},
		{
			"name": "update_mcp_server_user_access",
			"description": "Grant or revoke individual user access to an MCP server. Changes take effect immediately and override the default access setting for the selected users; use list_mcp_server_user_access first if you need the current state.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"id": {
						"type": "string",
						"description": "The MCP server ID or slug"
					},
					"users": {
						"minItems": 1,
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"user_id": {
									"type": "string",
									"description": "User ID"
								},
								"enabled": {
									"type": "boolean",
									"description": "Whether user has access"
								}
							},
							"required": ["user_id", "enabled"]
						},
						"description": "Array of user access updates"
					}
				},
				"required": ["id", "users"],
				"$schema": "http://json-schema.org/draft-07/schema#"
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": true
			}
		}
	]
}
